*Week of Mar 1–8 (638 commits)*

• *Guardrails Pipeline (Mar 1–3, 6–8) — Full stack, ground-up*
• *Compiler*: Tiered evaluation engine (Tier 1 CEL local, Tier 2 model-based with OpenAI Moderation/vLLM/TGI/Ollama providers, Tier 3 LLM-based), custom HTTP provider with SSRF protection and template interpolation, provider registry with circuit breaker protection, 10 CEL functions (PII, pattern, text analysis), result aggregator with action precedence, redact/fix/filter action executors
• *Runtime*: Wired guardrails at all 5 pipeline points (input, output, tool_input, tool_output, handoff), streaming guardrail evaluator with sentence buffering, cost tracking via Redis microdollars, exact-match result caching with Redis, policy CRUD with scope-based merging, guardrail inheritance with define rules for project-level overrides, trace events + webhook delivery, session policy resolution, re-validate tool params after guardrail modification
• *Studio*: Provider + policy creation UI with Form/YAML dual-mode, guardrails management page with provider form, E2E tests migrated from Tier 2 to LLM-based evaluation
• *IR*: Expanded Guardrail IR with kind, priority, tier, provider, severity support; GuardrailAction type system separate from ConstraintAction; compile-time validation for action+kind combinations
• 4 rounds of code review findings addressed across security, performance, resilience, types, and UI

• *Behavior Profiles (Mar 2–3)*
• New DSL construct: BEHAVIOR_PROFILE with USE BEHAVIOR_PROFILE references parsed and compiled to IR
• Profile resolver with context assembly and effective config builder integrated into execution pipeline
• Runtime emits profile_resolution trace events for observability
• Studio UI: behavior profile pages, store, and serializer; included in Arch AI context
• Project import/export support in project-io package
• E2E + round-trip tests, rebase compatibility issues resolved

• *Unified Agent Type & Per-Step Reasoning (Mar 2)*
• Deleted agent-level MODE, made GOAL required, added per-step REASONING zones
• Cascading fixes across compiler, parser, language-service, CLI, NL-parser, and test fixtures
• Updated all generator templates, section-splicer fixtures, and DOC_TOPICS
• Studio: removed scripted mode check from FLOW visibility, fixed mode field handling

• *Lookup Table Redesign (Mar 2)*
• Rewritten resolver with O(1) Set lookup, TTL cache, circuit breaker
• CSV/JSON upload endpoints with CRUD API routes for lookup table entries
• Fuzzy match confirmation and inference confirmation wired into flow-step-executor
• Normalized values computed at compile time for case-insensitive matching
• New LookupEntry DB model, Zod schema with enum sources and refinements
• Source types and properties renamed for platform abstraction
• Validation strengthened with extracted constants, field/source/timeout checks

• *Language Service Enhancements (Mar 1)*
• CEL function completions in expression contexts with metadata registry
• Field property and type value completions (mode, type, action, strategy, priority)
• YAML serialization exported from public API with test coverage
• Trigger characters and fetch available agents for ABL completions
• ABL extensions roadmap: browser validation, bundle export, docs site, LSP server, VS Code extension

• *Agent Transfer (Mar 5–8) — Full feature*
• *Compiler*: EscalationRouting IR schema extended, DSL parser support for PII/confirmation/extraction keywords
• *Runtime*: Full subsystem wiring with pipeline module, voice tool dispatch, VoiceToolResult routing, SmartAssist integration, dead letter store, trace adapter, graceful shutdown, session fields, Kore voice+chat production readiness
• *Studio*: Agent Transfer settings page (session, routing, voice, PII config), routing sub-section added to EscalationEditor with full ESCALATE serialization and IR parsing, transfer sessions monitoring page with filters and detail modal, agent desktop provider registry + creation flow, connection categorization with inline section headers
• Security, events, CSAT, and observability gaps closed
• Reliability fixes and runtime wiring hardened across multiple review rounds
• Design v4 document, channel gap analysis, and implementation plan

• *Evals System (Mar 5–7)*
• Phase 1 data foundation: Mongoose models, ClickHouse DDL for analytics, API routes, eval templates
• Phase 4: AI-generated personas and scenarios, quick eval with upsert, pipeline activities
• Studio: Full UI layer with run API routes, "Fix in Architect" workflow integration, eval suggestion toast, alert rules
• Preflight validation with circuit breaker diagnostics, Restate preflight service
• Auth extraction, error aggregation, and contract tests
• Retention scheduler overhaul, evaluator category enum fixes, empty state UX improvements
• Multiple code review rounds: tenant isolation, route validation, bounds checking, error handling

• *Workflow Engine (Mar 3–5)*
• Durable suspension implemented via Restate with Restate ctx threading through runWorkflow
• Loop and transform step executors, trigger engine with webhook fire endpoint
• Tool-call and async-webhook step types added to workflow editor
• Publish lifecycle controls: pause/activate/archive in Studio
• Human-in-the-loop system with workflow integration
• Connector wiring with dynamic loading in StepEditor, connectors made fully operational
• Studio creation modal with name/type/description, notification rules proxy
• Approval pagination, trigger list endpoint wired to Studio tab
• Step config denormalization, trigger field alignment, route shadowing fix

• *Decision Log System (Mar 8)*
• New DecisionEntry types and decisionLog field on RuntimeSession
• appendDecision() helper with verbosity gating
• Wired at 12 decision points: flow transitions, gather, validation, correction, constraints, guardrails, data mutations, handoff, completion, escalation, delegation, CALL AS mutations
• Persistence to MongoDB session document
• WebSocket state_update forwarding to Studio Observatory
• DecisionTreeView component for Observatory Decisions tab
• MCP debug: explain_decision enhanced with decision log support
• Review findings addressed: perf guards, PII redaction, memory cap, matched semantics
• Comprehensive test coverage: unit edge cases, integration emission points, multi-agent e2e

• *PII Redaction System (Mar 8)*
• *Compiler*: PII token vault with reversible tokenization, context-aware PII guard with gather field exemptions, selective redaction with type exemptions, custom regex extraction for gather fields, sensitive display renderer for gathered values, confirmation config added to ToolDefinition IR schema
• *Runtime*: Output PII redaction filter for agent responses, vault integration into session lifecycle, transient PII cleanup after gather completes, Phase 2 integration into reasoning executor, PII recognizer registry, audit logger
• *Studio*: UI for tool confirmation, PII fields, and extraction patterns; project-level PII config wiring
• Project-level PII redaction settings across runtime, studio, and database
• Comprehensive PII spec with full module inventory, phase tracking, and gap analysis

• *Session Lifecycle & Contact Context (Mar 3–5)*
• GDPR compliance with encryption at rest and data minimization
• Message retention policies with configurable TTLs
• Contact context persistence with cursor pagination and session compression
• Session resolver and conversation sync gap fixes
• SET-based session slot tracking with pre-generated sessionIds
• Tenant config types, encryption salt, contactId backfill resolved
• Multiple review rounds: session isolation, auth, cascade, encryption findings

• *Admin Portal (Mar 3–4)*
• Shared admin-ui package: DataTable, FilterBar, Tabs, ChartCard, ChartTooltip, DateRangePicker
• Tenant management: list/detail/status API with members and projects endpoints
• Deal CRUD with credit ledger, billing line items, and HubSpot actions
• 3-step model provisioning wizard with model detail page (settings + connections)
• System health dashboard with 17-service registry, cluster health monitoring
• Usage analytics page with charts and date range filtering
• Config overrides page with plan defaults comparison, resilience controls
• HubSpot CRM integration: webhook + manual sync endpoints, API client service
• SWR data fetching infrastructure, migrated all pages from useFetch
• Grouped sidebar navigation, E2E tests for tenant and deal lifecycles

• *Channel Manifest & SDK Channels (Mar 2–5)*
• ChannelManifest as single source of truth wired into types, routes, and DB model
• MSTeams, WhatsApp, and Messenger channel OAuth providers added
• Twilio webhook signature enforcement on /connect and /status routes
• Slack interactive form-encoded payload parsing
• Voice detection refactored to use ChannelManifest in prompt builder
• Duplicate webhook route removed, SDK channel CRUD consolidated
• SDK channels vs channel-connections route distinction documented

• *Export v2 / Project I/O (Mar 7–8)*
• Two-wave assembly with layer selection (evals, search, channels, vocabulary)
• v1→v2 migration handler with Arch export context
• 3-tier SHA lockfile generator, manifest generator v2 with layer metadata
• Git sync with Bitbucket dual-auth support, conflict strategy propagation
• Async BullMQ export queue, doctor API route for post-import validation
• CLI: export/import commands, git sync, verify, and doctor commands
• v2 layer checklist in Export dialog, format badges in Import dialog
• Cross-layer validator with case-insensitive tool matching
• SHA verification, CLI lockfile naming, staged import collection mapping fixed
• Tenant and project scoping added to export assembler queries
• Integration tests: round-trip, crash recovery, performance
• Multiple review rounds: security, correctness, coverage, dedup

• *Architecture Simplification — Sprint 3 (Mar 7–8)*
• *API verticalization*: extracted service/repo layers from pilot routes across multiple apps
• SearchAI connectors: 1,721 → 346 LOC (80% reduction)
• Studio Arch chat route: 1,209 → 129 LOC (89% reduction)
• Studio Arch generate route: 1,349 → 95 LOC (93% reduction)
• Error envelope standardized: { success: true/false, data/error: { code, message } }
• Shared package decomposition into shared-kernel, shared-auth, shared-observability
• Dependency-cruiser boundary rules added in warn mode
• Architecture baseline metrics captured for simplification plan
• 12 broken search-ai tests repaired (ESM imports, db/index mocks, auth-repo mocks)

• *Runtime Unification — Sprint 2 (Mar 7–8)*
• Execution context bridge mapping RuntimeSession → ExecutionContext
• 7 construct executors wired: GatherExecutor, ConstraintExecutor, CompletionDetector, HandoffExecutor, DelegateExecutor, FlowExecutor, ReasoningExecutor
• Shadow mode infrastructure added for safe rollout then removed after parity confirmed (-1,537 lines)
• Completion traces restored via onCheck callback to CompletionDetector
• Handoff validation reordered: self-handoff → cycle → registry → allowed targets
• 45 review findings resolved across 5 review agents (security, logic, infrastructure)
• Gather validation and constraint action parity tests, trace emission parity tests
• 29 over-excluded passing tests restored to fast tier

• *NLU Tiered Architecture (Mar 4)*
• Tier 1 JS extractors for email, currency, and number built into runtime
• Tier 2 ML sidecar gated on nlu_provider === 'advanced' project config
• Enterprise entitlement gate with advancedNlu plan feature
• Studio NLU provider toggle with enterprise gate in RuntimeConfigTab
• NluProvider type and fields added to ProjectRuntimeConfigIR
• NLU sidecar client moved from global to per-session via project config

• *Topology Sizing Calculator (Mar 4)*
• Tier classifier, service sizer, compute sizer, and data store sizer engines
• Disk growth calculator, managed recommender, and orchestrator
• Helm values generator with sizing CLI commands
• k6 benchmarks: runtime, BGE-M3, data stores, application services, system-wide stress
• k6 operator manifests, per-tier Helm values
• Grafana dashboards for benchmark visualization
• Production topology sizing design doc and operational runbook

• *Feature Gating & Billing (Mar 4)*
• Feature catalog with tenant features endpoints
• Feature gate middleware for runtime route protection
• Workspace billing API routes with billing line items
• Studio billing page with proxy, hooks, and alert configuration
• Studio feature gating applied to admin sidebar navigation

• *Studio UX (Mar 1, 4, 7)*
• Vercel-style drill-down sidebar navigation redesign
• Sidebar restructured into Build/Resources/Insights/Operate/Govern sections
• Observatory consolidation from 10 tabs to 5
• Agent editor typography, spacing, and motion polish
• Glass header, explicit button labels, collapsible errors, menu accents
• Card shadows, stagger animations, and bg-noise texture
• Resizable symbol tree panel with search filter
• Diagnostics source filter tabs, error boundaries for all route segments
• Settings refactored into sidebar nav group with sub-page routing through AppShell

• *CLI & MCP Tools (Mar 2, 5, 7)*
• Analysis MCP tools: explain_dsl, suggest_improvements, test_agent
• Model management tools and commands registered in CLI and MCP server
• Authoring and testing MCP tools wired into server
• Agent commands with remote platform E2E test and agent compile route
• apx CLI for managing platform services with setup guide updates
• MCP debug: explain_decision enhanced with decision log support
• 6 SDLC skills and 3 automation tools for repeatable development workflows
• CLI defaults API routing to Studio gateway
