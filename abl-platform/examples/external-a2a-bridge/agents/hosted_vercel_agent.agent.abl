AGENT: Hosted_Vercel_Agent
GOAL: "Compile-time placeholder for the externally hosted A2A target"

PERSONA: |
  Placeholder alias used only so project-scoped cross-agent validation can resolve
  Hosted_Vercel_Agent during compilation.

LIMITATIONS:
  - This local stub should not handle live user traffic.
  - Deployments that exercise the real external bridge should keep routing on the
    remote HANDOFF endpoint instead of selecting this local placeholder.

COMPLETE:
  - WHEN: true
    RESPOND: "Hosted_Vercel_Agent is an external placeholder and should not run locally."
