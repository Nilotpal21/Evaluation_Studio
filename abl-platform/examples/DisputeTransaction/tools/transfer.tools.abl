# Transfer Tools
# These tools are defined inline in the agent DSL files.
# This file serves as a tool registry reference for the v2 export format.

# Tools:
#   check_agent_availability — Check if human agents are available in the requested department
#   create_transfer_ticket — Create a transfer ticket with customer context
#   initiate_transfer — Initiate the transfer to a human agent
