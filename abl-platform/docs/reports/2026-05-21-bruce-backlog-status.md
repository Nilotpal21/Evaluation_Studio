# Bruce Feedback Consolidated Current Status Report

**Audit date:** 2026-05-21  
**Code basis:** current `develop` checkout as recovered on 2026-05-21  
**Purpose:** one compact Markdown report covering the canonical Bruce feedback source chain found in `projects` and `Downloads`.

## Sources

| Source                 | Path                                                                                                    | Rows in this report |
| ---------------------- | ------------------------------------------------------------------------------------------------------- | ------------------- |
| Mar 3-10 feedback      | /Users/prasannaarikala/projects/tmp/Bruce_Wilcox_Feedback_Mar3-10_2026.txt                              | 46                  |
| Mar 20-21 code review  | /Users/prasannaarikala/Downloads/Bruce_Wilcox_Feedback_Mar20-21_2026.docx                               | 28                  |
| Mar 22-Apr 18 feedback | /Users/prasannaarikala/Downloads/Bruce_Wilcox_Feedback_Mar22-Apr18_2026.md                              | 95                  |
| Apr 19-May 21 feedback | /Users/prasannaarikala/projects/f-2/abl-platform/docs/reports/Bruce_Wilcox_Feedback_Apr19-May21_2026.md | 43                  |

## Status Legend

| Status                     | Meaning                                                                                                                |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| Addressed                  | Current code, tests, or docs show the item is implemented/resolved.                                                    |
| Partially addressed        | Core fix exists, but a known follow-up remains.                                                                        |
| Open / verified gap        | A focused second-pass check found current code/docs evidence that the gap remains.                                     |
| Open / no verified closure | No verified closure trail was found in this compact-safe recovery pass. Treat as not addressed until proven otherwise. |
| Parked / accepted debt     | Explicitly accepted or parked debt.                                                                                    |
| Addressed by design close  | Closed by an explicit product/architecture decision rather than an implementation.                                     |
| No action item             | Feedback was comparative, positive, recanted, or otherwise not an actionable remediation item.                         |

## Status Summary

| Status                     | Count |
| -------------------------- | ----- |
| Addressed                  | 130   |
| Partially addressed        | 35    |
| Open / verified gap        | 37    |
| Open / no verified closure | 0     |
| Parked / accepted debt     | 2     |
| Addressed by design close  | 4     |
| No action item             | 4     |

## Recovery Notes

- The previous thread failed during remote context compaction after very large `rg` outputs. This recovery kept evidence concise and completed the remaining audit in bounded batches.
- The 2026-05-21 second pass upgraded only rows with direct, bounded evidence. No rows remain marked `Open / no verified closure`; remaining open rows now have specific verified evidence.
- The strongest verified closure evidence is for the March 20-21 narrowed remaining-items plan and the April Bruce partial-items closure docs.
- `Open / no verified closure` remains in the legend for provenance, but its current count is zero after the focused recovery audit.
- No build or test suite was run for this report-only recovery.

## Mar 3-10 feedback

| ID   | Severity | Item                                              | Current status            | Recovery note                                                                                                                                |
| ---- | -------- | ------------------------------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1  | -        | Tool Call Parameter Validation                    | Addressed                 | Tool schema/parameter validation exists in compiler/core validation tests.                                                                   |
| 1.2  | -        | Security: LLM-Driven Data Manipulation            | Partially addressed       | Side-effecting tool confirmation is now warning-only; runtime auto-confirmation remains intentionally absent.                                |
| 2.1  | -        | RESPOND Messages Never Reach the User             | Addressed                 | `ON_ERROR RESPOND` is wired through runtime response/streaming paths.                                                                        |
| 2.2  | -        | LOG Directive Not Implemented                     | Open / verified gap       | Bounded parser/compiler/runtime searches found no DSL `LOG` directive/action implementation.                                                 |
| 2.3  | -        | DELEGATE vs HANDOFF Semantic Confusion            | Addressed                 | Handoff/delegate semantics are now split in docs and runtime contracts.                                                                      |
| 2.4  | -        | Error Type System Is Tool-Error-Only              | Addressed                 | Runtime-local tool error classifier is implemented and tested.                                                                               |
| 3.1  | -        | Remember Triggers Fire Repeatedly                 | Addressed                 | REMEMBER writes are deduplicated with read-before-write/deep-equal tests.                                                                    |
| 3.2  | -        | Recall Timing Gaps (Pre-Action Missing)           | Addressed by design close | `tool:*:before` recall remains unsupported by explicit contract because recall can mutate dispatch context.                                  |
| 3.3  | -        | Escalate Context: Undefined Sources               | Open / verified gap       | Runtime `filterEscalationContext` still silently skips missing `context_for_human` fields.                                                   |
| 4.1  | -        | Bug: goto_step Target Field Mismatch              | Addressed                 | `goto_step` target-field mismatch has current compiler/runtime coverage.                                                                     |
| 4.2  | -        | Phase Information Discarded at Compile Time       | Addressed                 | Phase-aware handoff/error semantics are now represented in current docs/tests.                                                               |
| 4.3  | -        | retry_step Has No Backtrack Limit                 | Open / verified gap       | `goto_step` enforces `MAX_BACKTRACKS_PER_STEP`; `retry_step` still returns retry without an equivalent count/limit.                          |
| 4.4  | -        | Short-Circuit Hides Multiple Violations           | Open / verified gap       | Normal constraint execution still returns on the first failed constraint; only the `recordOnly` path collects all results.                   |
| 4.5  | -        | Variable Extraction Regex False Positives         | Addressed                 | Auto-guard variable extraction has regression coverage for legacy operators/false positives.                                                 |
| 4.6  | -        | Backtrack Counts Never Reset                      | Open / verified gap       | `backtrackCounts` is incremented in execution code; no reset/delete path was found in execution services.                                    |
| 5.1  | -        | Reask Action Defined But Never Executed           | Addressed                 | Runtime reask regeneration loop and retry-limit tests exist.                                                                                 |
| 5.2  | -        | Dual Evaluation Path (Input Guardrails Run Twice) | Addressed                 | Duplicate input guardrail evaluation is covered by regression tests.                                                                         |
| 5.3  | -        | result.passed Semantics Inconsistent              | Partially addressed       | `modifiedContent` exists, but no verified explicit `modified` boolean on the pipeline result contract.                                       |
| 5.4  | -        | Action Applier Uses Wrong Action Map              | Addressed                 | Guardrail action application honors resolved severity actions.                                                                               |
| 5.5  | -        | Streaming Evaluator: Chunks in Isolation          | Parked / accepted debt    | Streaming evaluator still evaluates only unevaluated chunks; full-buffer evaluation remains parked.                                          |
| 5.6  | -        | Provider Registry TTL Drops Providers Mid-Session | Addressed                 | DB-loaded guardrail providers are session-permanent and reload by explicit invalidation.                                                     |
| 5.7  | -        | Cost Tracking Disconnected from Pipeline          | Addressed                 | Guardrail budget/cost tracking closure is documented as completed in the March backlog follow-up.                                            |
| 5.8  | -        | Guardrails Spec Documentation Gaps                | Addressed                 | Guardrail docs were expanded in the reference spec and status docs.                                                                          |
| 6.1  | -        | ON_INPUT: Wrong Tool for NLU                      | Addressed                 | `ON_INPUT` is documented as deterministic routing, not LLM classification.                                                                   |
| 6.2  | -        | Digression Intent Detection Too Cheap             | Partially addressed       | Main intent detection has boundary tests; NLU fallback still has simple substring matching paths.                                            |
| 6.3  | -        | CALL Syntax Redundant                             | Addressed                 | CALL syntax cleanup is documented in the reference docs.                                                                                     |
| 6.4  | -        | Bad ABL Spec Code Examples                        | Partially addressed       | Several examples were refreshed, but the full example inventory was not re-audited in this recovery pass.                                    |
| 6.5  | -        | ON\_ Labels Documentation Missing                 | Addressed                 | `ON_*` label documentation was added to the reference docs.                                                                                  |
| 6.6  | -        | Implicit Global Variables Not Documented          | Addressed                 | `ABL_SPEC.md` now has a canonical runtime context variable table covering built-in/system variables and dotted `user.*` metadata boundaries. |
| 6.7  | -        | Undocumented EXECUTION Section                    | Addressed                 | EXECUTION section documentation was added.                                                                                                   |
| 7.1  | -        | COLLECT/GATHER: No Custom Extractors              | Partially addressed       | Pattern/entity extraction exists, but arbitrary custom extractor plugin support is not verified.                                             |
| 7.2  | -        | No Custom Regex Entities for XO11 Migration       | Partially addressed       | Regex/entity migration support exists in parts; full XO11 custom entity parity is not verified.                                              |
| 8.1  | -        | PII Redaction Is Context-Blind                    | Partially addressed       | `PII_TYPE` supports non-canonical gathered fields; lower-level trace/observation masking follow-up remains.                                  |
| 9.1  | -        | Critical: SearchDocument Query Missing tenantId   | Addressed                 | Search document tenant scoping was included in prior closure work.                                                                           |
| 9.2  | -        | IndexRegistry Missing Tenant Isolation Plugin     | Addressed                 | Index registry tenant isolation was included in prior closure work.                                                                          |
| 9.3  | -        | SharedIndexTracker: No tenantId at All            | Addressed                 | Shared index tracking tenant isolation was included in prior closure work.                                                                   |
| 9.4  | -        | findById() Pattern Fragile                        | Partially addressed       | Direct production `Model.findById()` usage appears removed/blocked, but deprecated unscoped `BaseModel.findById()` remains.                  |
| 9.5  | -        | WebSocket tenantId from Client                    | Addressed                 | Current platform guidance requires centralized auth and server-derived tenant identity.                                                      |
| 10.1 | -        | Handoff Missing ON_FAILURE                        | Addressed                 | Handoff `ON_FAILURE` is documented and tested with phase-aware semantics.                                                                    |
| 10.2 | -        | Compiler Soft-Fails on Missing Targets            | Addressed                 | Missing target/compiler diagnostic handling was hardened in prior closure work.                                                              |
| 11.1 | -        | Classifier is LLM-Only, No Cheaper Alternatives   | Partially addressed       | Classifier strategy exists in parts, but a full user-selectable cheap/supervisor classifier stack is not verified.                           |
| 12.1 | -        | Positive Assessment                               | No action item            | Positive assessment; no remediation item.                                                                                                    |
| 12.2 | -        | Feature Request: Chunking Strategy Comparison     | Open / verified gap       | Bounded source search found design/test-tracker mentions, but no product UI/API implementation for chunking comparisons.                     |
| 13.1 | -        | Cognigy vs ABL: Key Gaps Identified               | No action item            | Comparative Cognigy assessment; no direct code remediation item.                                                                             |
| 13.2 | -        | Rasa vs ABL: Architectural Philosophy Differences | No action item            | Comparative Rasa assessment; no direct code remediation item.                                                                                |
| 15.1 | -        | Saludsa: Language Ambiguity Risk                  | Partially addressed       | Language fields compile, but some runtime/default paths still fall back to hardcoded `en`.                                                   |

## Mar 20-21 code review

| ID   | Severity | Item                                                                    | Current status         | Recovery note                                                                                                               |
| ---- | -------- | ----------------------------------------------------------------------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 1.1  | -        | Filter Expression Parser Breaks on Operator-Like Characters in Strings  | Addressed              | Filter parser now uses top-level/string-aware comparison splitting.                                                         |
| 2.1  | -        | HALF_OPEN State Machine Is Fundamentally Broken (2 Compounding Bugs)    | Addressed              | Circuit-breaker half-open state has regression coverage.                                                                    |
| 3.1  | -        | Log Redactor Field Matching Is Case-Sensitive                           | Addressed              | Log redactor field matching normalizes keys case-insensitively.                                                             |
| 3.2  | -        | Encryption Silently Downgrades to Plaintext on Failure                  | Addressed              | Encryption failure handling was part of the prior ABLP-188 closure path.                                                    |
| 3.3  | -        | Off-by-One in SmartAssist Retry Loop                                    | Open / verified gap    | Current `SmartAssistClient` retry loop still uses `attempt <= maxAttempts`, allowing one extra attempt.                     |
| 3.4  | -        | Lua Script JSDoc Is Dangerously Wrong                                   | Addressed              | Lua script docs now describe the single-key session-hash contract and caller-pipelined cross-slot writes.                   |
| 3.5  | -        | Deflect-to-Chat Branching Doesn’t Match Documented XO Logic             | Open / verified gap    | Current `resolveBranch` still sends `deflectionType: automation` with omitted `triggerType` to agent transfer.              |
| 3.6  | -        | CSAT Handler ‘return’ Action Sets State to ‘ended’                      | Partially addressed    | Voice CSAT now has `post_agent` ownership and tests; the generic return-to-bot branch still needs focused audit.            |
| 3.7  | -        | Log Redactor Doesn’t Recurse into Arrays                                | Addressed              | Log redactor recursively handles arrays/objects.                                                                            |
| 4.1  | -        | oneOf/anyOf Union Crashes on Single-Item Arrays                         | Addressed              | Single-item union conversion was treated as fixed/out-of-scope in the narrowed remaining plan.                              |
| 5.1  | -        | NOT IN Is Broken — Will Always Mis-Parse as IN                          | Addressed              | `NOT IN` parsing was treated as fixed/out-of-scope in the narrowed remaining plan.                                          |
| 5.2  | -        | splitByOperator Doesn’t Respect String Literals                         | Addressed              | String-literal-aware operator splitting was treated as fixed/out-of-scope.                                                  |
| 5.3  | -        | parseComparisonExpression Also Doesn’t Respect Strings                  | Addressed              | String-literal-aware comparison parsing was treated as fixed/out-of-scope.                                                  |
| 5.4  | -        | expressionToPython Unary Switch Has No Default — Silent Fall-Through    | Addressed              | Unary conversion default handling was treated as fixed/out-of-scope.                                                        |
| 5.5  | -        | Duplicated Tool Parsing Functions                                       | Addressed              | Duplicated tool parsing cleanup was treated as fixed/out-of-scope.                                                          |
| 6.1  | -        | Dockerfile HEALTHCHECK URL Is Wrong                                     | Addressed              | Admin healthcheck route/Dockerfile behavior has test coverage.                                                              |
| 6.2  | -        | Error Detail Leakage in Secret Rotation                                 | Addressed              | Secret-rotation error sanitization has current route coverage.                                                              |
| 7.1  | -        | Image Base64 Encoding Produces Garbage                                  | Addressed              | Docling image serialization was fixed in ABLP-188 closure commits.                                                          |
| 7.2  | -        | Pydantic .dict() Deprecated                                             | Addressed              | Docling Pydantic v2 serialization was fixed in ABLP-188 closure commits.                                                    |
| 8.1  | -        | Session Lock Fails Open on Redis Outage                                 | Addressed              | Runtime session locking exists with Redis SET NX/Lua release coverage.                                                      |
| 8.2  | -        | Streaming Guardrail Leak Window                                         | Parked / accepted debt | Streaming guardrail leak/full-buffer behavior remains parked/accepted debt.                                                 |
| 9.1  | -        | Entry Agent Determined by Iteration Order — Silent Last-Wins            | Addressed              | Entry-agent diagnostics/order handling was fixed in ABLP-188 closure commits.                                               |
| 10.1 | -        | streamComplete / streamCompleteWithTools Are Synchronous                | Open / verified gap    | Arch `streamComplete*` still awaits full completion and yields a single `text_delta`; no true incremental stream.           |
| 10.2 | -        | encryptedApiKey Used as Plaintext                                       | Addressed              | Studio credential reads avoid `.lean()`, and Arch resolves `encryptedApiKey` via `resolveTenantPlaintextValue`.             |
| 10.3 | -        | applyDiff Extracts projectId Incorrectly for Agent-Scoped Conversations | Addressed              | Legacy `arch-store`/`projectIdFromConvKey` path is gone; `apply_modification` now uses the authenticated session projectId. |
| 10.4 | -        | No Tenant Isolation in executeQuerySessionTraces                        | Addressed              | Query-session-traces tenant isolation was treated as fixed/out-of-scope in the narrowed remaining plan.                     |
| 10.5 | -        | parseJsonFromResponse Regex Is Greedy                                   | Addressed              | `parseJsonFromResponse` no longer exists in current Studio source; only archived plans and this report reference it.        |
| 11.1 | -        | Pause / WaitIfPaused Promise Conflict                                   | Addressed              | Observatory pause/wait conflict has regression tests.                                                                       |

## Mar 22-Apr 18 feedback

| ID    | Severity | Item                                                                                                                                 | Current status            | Recovery note                                                                                                                                                                           |
| ----- | -------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| 1.1   | CRITICAL | Guard constraint expansion uses OR instead of AND — allows invalid states                                                            | Addressed                 | `autoGuardConstraint` now joins guard predicates with `AND` before OR-ing the original condition.                                                                                       |
| 1.2   | HIGH     | CONTAINS operator generates backwards Python code                                                                                    | Addressed                 | `expression-migrator` maps uppercase `CONTAINS` to receiver `.contains(...)`, with CEL parity coverage.                                                                                 |
| 1.3   | MEDIUM   | parseDefaultValue doesn't handle negative numbers                                                                                    | Addressed                 | Numeric default parsing now accepts leading `-` and parses matching values as numbers.                                                                                                  |
| 1.4   | MEDIUM   | Escape sequence handling wrong for double-escaped backslashes                                                                        | Addressed                 | Expression parsing now counts preceding backslashes, with regression coverage for double-escaped strings.                                                                               |
| 1.5   | MEDIUM   | StepNumber regex ambiguity with NumberLiteral                                                                                        | Addressed                 | Lexer `StepNumber` now requires a dot not followed by a digit; `1.5.` tokenization is covered.                                                                                          |
| 1.6   | HIGH     | No CEL syntax validation in compilation pipeline                                                                                     | Open / verified gap       | Compiler validation still scans operators only; no compile-time CEL syntax parse/validation path was found.                                                                             |
| 1.7   | HIGH     | Flow step skips parsing/compiling on_error — silently dropped                                                                        | Partially addressed       | Flow-step `ON_ERROR` is parsed and present in schema, but `compileFlow` still does not emit `step.onError` into IR.                                                                     |
| 1.8   | MEDIUM   | Agent-level on_error parsing captures only 4 of 10 fields                                                                            | Partially addressed       | Parser/compiler now cover most retry/subtype/backtrack fields, but direct `HANDOFF_TARGET` syntax remains unsupported.                                                                  |
| 1.9   | MEDIUM   | Unreachable branches not detected                                                                                                    | Open / verified gap       | IR validation has unreachable action/flow-step checks, but no branch-order diagnostic for ELSE-before-IF style cases.                                                                   |
| 1.10  | HIGH     | DELEGATE requires RETURNS but only checks at runtime                                                                                 | Addressed                 | Delegate parsing now defaults `returns` to `{}`, and tests accept empty `RETURNS`; the contract is optional rather than late-failing.                                                   |
| 1.11  | HIGH     | AGENTS section parser is a stub — no validation                                                                                      | Open / verified gap       | `parseAgentsSection` in the agent-based parser still skips the section; separate supervisor-parser coverage does not close it.                                                          |
| 1.12  | HIGH     | Circular depends_on creates phantom completion                                                                                       | Addressed                 | Field-reference validation now detects `depends_on` cycles and reports `CIRCULAR_DEPENDS_ON`.                                                                                           |
| 1.13  | HIGH     | Compiler doesn't cross-check field names in COLLECT                                                                                  | Addressed                 | IR validation cross-checks `on_fail.collect_fields` against gather fields and emits invalid-field diagnostics.                                                                          |
| 1.14  | HIGH     | ON_FAIL compiles to single action, not a sequence                                                                                    | Addressed                 | Structured `ON_FAIL` blocks now support `COLLECT` plus follow-up `GOTO`/`THEN` via `then_action`/`then_step`.                                                                           |
| 1.15  | HIGH     | Unrecognized template channel names silently skipped                                                                                 | Open / verified gap       | Template parsing still only accepts known format keys; unknown keys are skipped without a diagnostic.                                                                                   |
| 1.16  | MEDIUM   | Templates should error if defined in FLOW step                                                                                       | Open / verified gap       | FLOW step parsing still lacks `TEMPLATE(S)` support and still skips unknown step keys without a parser diagnostic.                                                                      |
| 2.1   | CRITICAL | randomCache is global memory leak storing raw PII as keys                                                                            | Addressed                 | `PIIVault` now keeps `randomCache` instance-scoped and keyed by `tokenId`; the legacy global clear helper is a no-op shim.                                                              |
| 2.2   | HIGH     | PIIAuditLog not deleted in cascade-delete — GDPR Article 17 risk                                                                     | Addressed                 | Cascade delete removes `PIIAuditLog` by tenant/project, and module cascade tests assert the cleanup calls.                                                                              |
| 2.3   | HIGH     | Tools consumer sees original PII by default                                                                                          | Addressed                 | Tools consumers now resolve PII render mode to `redacted` by default instead of `original`.                                                                                             |
| 2.4   | HIGH     | Conversation history stores raw PII in Redis                                                                                         | Addressed                 | Redis session history list entries now encrypt/decrypt via tenant DEK wiring, and Redis startup fails when encryption is unavailable.                                                   |
| 2.5   | HIGH     | PIIDetection.value carries raw PII through call stack                                                                                | Addressed                 | `PIIDetection.value` is now a safe type label/preview rather than the matched raw substring.                                                                                            |
| 2.6   | MEDIUM   | PII streaming buffer assumes 40 chars is enough                                                                                      | Addressed                 | Streaming PII buffering now keeps a 320-character trailing window for cross-chunk detection.                                                                                            |
| 2.7   | HIGH     | No PII-in-error-messages guard — Zod validation errors echo raw input                                                                | Open / verified gap       | Multiple runtime and Studio routes still return `parsed.error.message` or `parsed.error.issues` directly to clients.                                                                    |
| 2.8   | HIGH     | Unexpected PII in user input gets fully logged                                                                                       | Addressed                 | Current regression tests lock logger/session persistence redaction for emails, phones, secret-like strings, and custom PII patterns.                                                    |
| 2.9   | MEDIUM   | Trace scrubber relies on regex only, not vault awareness                                                                             | Partially addressed       | Trace scrubbing now accepts custom recognizers and upstream tokenized inputs, but the scrubber itself is still not vault-token aware.                                                   |
| 2.10  | HIGH     | System-injected keys (user_id) are mutable at runtime                                                                                | Partially addressed       | Compiler validation warns on immutable system identifiers, but runtime SET/tool-memory write paths still do not fail closed.                                                            |
| 3.1   | HIGH     | WHEN clause silently dropped for checkpoint constraints                                                                              | Addressed                 | Constraint compilation now folds `WHEN` into checkpoint-gated conditions and preserves `applies_when` metadata.                                                                         |
| 3.2   | HIGH     | RESTRICT negation inconsistent with BEFORE                                                                                           | Addressed                 | Constraint compilation now negates `RESTRICT` before adding `WHEN`/`BEFORE` checkpoint gating.                                                                                          |
| 3.3   | HIGH     | GOTO_STEP with nonexistent step not caught at compile time                                                                           | Addressed                 | IR validation now checks constraint and guardrail `goto_step` / `then_step` targets and emits dangling-step diagnostics.                                                                |
| 3.4   | HIGH     | kind: 'both' only works at compile time                                                                                              | Addressed                 | Runtime guardrail-policy normalization now expands `kind: 'both'` into input and output rules before persistence.                                                                       |
| 3.5   | HIGH     | LLM message returned with tool call bypasses guardrails                                                                              | Addressed                 | Reasoning execution buffers tool-call text when output guardrails/response constraints must run, then flushes only after checks pass.                                                   |
| 3.6   | MEDIUM   | CLEAR missing from ON_INPUT/ON_RESULT branches                                                                                       | Open / verified gap       | `InputBranch` and `CallResultBranch` still lack `clear`, and ON_INPUT/ON_RESULT branch parsing/runtime application do not execute it.                                                   |
| 3.7   | MEDIUM   | ON_RESULT vs ON_SUCCESS evaluation order undocumented                                                                                | Addressed                 | `ABL_SPEC.md` now documents first-match ON_RESULT evaluation and fallthrough to ON_SUCCESS/ON_FAIL.                                                                                     |
| 4.1   | HIGH     | pipeline property not parsed from DSL — silently dropped                                                                             | Addressed                 | `EXECUTION.pipeline` is now parsed into AST/IR with parser and compiler regression tests.                                                                                               |
| 4.2   | MEDIUM   | Tool filter: JSON parse failure = "no tools needed"                                                                                  | Open / verified gap       | `parseToolFilterResponse()` still returns `[]` on JSON parse failure, indistinguishable from an intentional no-tools selection.                                                         |
| 4.3   | MEDIUM   | Tool filter gives classifier only names, no descriptions                                                                             | Open / verified gap       | The tool-filter prompt still receives only tool names and formats `Available tools` as a comma-separated name list.                                                                     |
| 4.4   | MEDIUM   | selectedTools.length > 0 check masks 0-tools case                                                                                    | Addressed                 | `selectedTools` now applies an empty array as an empty set, leaving only pinned/system tools for explicit zero-tool selections.                                                         |
| 5.1   | CRITICAL | Delegate timeout doesn't prevent child from corrupting parent state                                                                  | Addressed                 | Delegate execution now uses a child session plus abort-signal timeout cleanup that clears/severs child state before deleting it.                                                        |
| 5.2   | HIGH     | Escalation triggers declared in DSL never evaluated at runtime                                                                       | Open / verified gap       | Compiler preserves `trigger.when`, but prompt-building drops it and no runtime evaluator for escalation trigger conditions was found.                                                   |
| 5.3   | MEDIUM   | Invalid escalate priorities silently become 'medium'                                                                                 | Partially addressed       | Escalation tool schema now exposes a priority enum, but `handleEscalate` still silently normalizes invalid direct inputs to `medium`.                                                   |
| 5.4   | MEDIUM   | No trace event for child→parent value overwrite                                                                                      | Open / verified gap       | `thread_return` is emitted before mapping, and `mapDelegateReturns()` still overwrites parent values without a dedicated trace event.                                                   |
| 5.5   | HIGH     | Delegate missing auth validation (handoff has it)                                                                                    | Addressed                 | Delegate now runs `validateDelegateAuthRequirements()` and emits a blocked `delegate_start` trace on auth preflight failure.                                                            |
| 5.6   | MEDIUM   | Callee agent doesn't run input guardrails on inherited messages                                                                      | Addressed                 | Delegate child execution calls `executeMessage` without `skipInputGuardrails`; reasoning and flow input guardrails still run.                                                           |
| 6.1   | HIGH     | REMEMBER is fire-and-forget with no retry                                                                                            | Open / verified gap       | REMEMBER writes still catch store failures, log `REMEMBER store failed`, and continue without retry or queueing.                                                                        |
| 6.2   | HIGH     | No sensitive flag on MEMORY declarations                                                                                             | Partially addressed       | `SENSITIVE`, `SENSITIVE_DISPLAY`, and `MASK_CONFIG` parse into AST/IR with tests, but REMEMBER writes do not pass sensitivity metadata.                                                 |
| 6.3   | HIGH     | REMEMBER WHEN still fires repeatedly (confirmed unfixed)                                                                             | Addressed                 | REMEMBER dedup landed with read-before-write/deep-equal tests.                                                                                                                          |
| 6.4   | MEDIUM   | persistent: field has no validation                                                                                                  | Partially addressed       | REMEMBER/RECALL persistent refs and read/write access are validated, but unused persistent declarations are still not diagnosed.                                                        |
| 7.1   | HIGH     | DIV null-safety lost in CEL path                                                                                                     | Open / verified gap       | `DIV(a, b)` still migrates to `a / b`; no CEL `abl.div()` null-safe helper was found despite the spec's null guarantee.                                                                 |
| 7.2   | HIGH     | RECALL not called at runtime for session:end, step:enter/exit events                                                                 | Addressed by design close | Tool-before recall remains explicitly unsupported by contract.                                                                                                                          |
| 7.3   | MEDIUM   | ON_ERROR ESCALATE silently dropped when inline                                                                                       | Open / verified gap       | `parseErrorHandler()` still does not accept inline `ESCALATE`; only `THEN: ESCALATE` reaches compiled error handling.                                                                   |
| 8.1   | HIGH     | robots.txt fetched but never enforced                                                                                                | Addressed                 | Bulk crawl checks `RobotsChecker.isAllowed()` and applies `getCrawlDelay()` when enabled; the Go crawler also respects robots.txt.                                                      |
| 9.1   | HIGH     | Deprecated auth pattern — tenant-attachment-config route is broken                                                                   | Open / verified gap       | Runtime platform-admin route is secured, but the Admin proxy still uses deprecated header-derived auth/runtime-header helpers.                                                          |
| 10.1  | MEDIUM   | Metadata extraction silently degrades                                                                                                | Partially addressed       | PDF/DOCX/PPTX metadata extraction, logging, and tests exist, but extractor failures still degrade to null/default metadata.                                                             |
| 10.2  | MEDIUM   | Image filtering heuristics hardcoded to US Letter                                                                                    | Open / verified gap       | `should_include_image()` still defaults to 612x792 and extraction calls it without passing actual page dimensions.                                                                      |
| 11.1  | MEDIUM   | Language inconsistency — agents don't declare delegation targets                                                                     | Partially addressed       | Delegate targets are validated from `coordination.delegates` and toolized, but regular agents still lack an `AGENTS:` contract.                                                         |
| 12.1  | -        | Return value behavior inconsistent between reasoning and scripted mode                                                               | Open / verified gap       | Spec documents `last_<tool_name>_result`, but no docs/tests were found for reasoning-vs-scripted return conversion differences.                                                         |
| 12.2  | -        | SET not available in reasoning mode for computed values                                                                              | Partially addressed       | Reasoning agents can update state via `__set_context__`, but no DSL `SET` expression/action surface exists for deterministic computes.                                                  |
| 12.3  | -        | ON_RETURN syntax inconsistency                                                                                                       | Open / verified gap       | Handoff `ON_RETURN` structured action/handler mapping remains separate from tool `ON_RESULT SET`; no unifying closure was found.                                                        |
| 12.4  | -        | GOAL should be documented as multiline                                                                                               | Addressed                 | `ABL_SPEC.md` now documents `GOAL` as a string or multiline block and explains when to use `GOAL:                                                                                       | `.  |
| 12.5  | -        | Constraints phase label documentation unclear                                                                                        | Addressed                 | `ABL_SPEC.md` now states constraint labels are arbitrary organizational groupings and have no runtime control effect.                                                                   |
| 12.6  | -        | LOOKUP_TABLES not documented in spec                                                                                                 | Addressed                 | `ABL_SPEC.md` now includes a `LOOKUP_TABLES` reference-data section with syntax, fields, and the experimental agent-local warning.                                                      |
| 12.7  | -        | RECALL event names in spec don't match runtime                                                                                       | Addressed                 | `ABL_SPEC.md` now documents canonical `ON: session:start`/`tool:<name>:after` events and rejects retired `ON_<event>` aliases.                                                          |
| 12.8  | -        | RESUME: true ordering concern                                                                                                        | Addressed                 | The digression example now shows `CLEAR` before `RESUME: true`, making the reset-before-resume order explicit.                                                                          |
| 12.9  | -        | QUICK REF vs ABL_SPEC syntax divergence                                                                                              | Partially addressed       | Built-in function count wording was normalized across `ABL_QUICK_REFERENCE.md` and `ABL_SPEC.md`; broader quick-ref-only construct drift still needs a full parity pass.                |
| 12.10 | -        | REQUIRED field redundant when DEFAULT is set                                                                                         | Addressed                 | Compiler warning `W822` now flags GATHER fields that set both `required:false` and `default` because defaults satisfy missing checks.                                                   |
| 12.11 | -        | GUARDRAILS msg field purpose ambiguous                                                                                               | Addressed                 | `ABL_SPEC.md` now has a Guardrail Fields table defining `msg` as a user-safe violation message/trace summary.                                                                           |
| 12.12 | -        | INSTRUCTIONS field at wrong nesting level in spec                                                                                    | Addressed                 | `EXPECT_RETURN` compatibility/docs were updated.                                                                                                                                        |
| 12.13 | -        | last\_<tool_name>\_result type not documented for non-object returns                                                                 | Addressed                 | `ABL_SPEC.md` now documents `last_<tool_name>_result` as the full result object after tool execution.                                                                                   |
| 12.14 | -        | Handoff history strategy — receiving agent data access undefined                                                                     | Addressed                 | `ABL_SPEC.md` now documents handoff `history` modes, memory grants, preserved child history, and explicit `PASS` for gathered values.                                                   |
| 12.15 | -        | NLU keyword undocumented but parsed; MULTI_INTENT, LOOKUP_TABLES, INSTRUCTIONS, INTENTS, MESSAGES, TESTS also parsed but not in spec | Addressed                 | `ABL_SPEC.md` now documents compatibility/runtime contracts for `NLU`, `INTENTS`, `MULTI_INTENT`, `MESSAGES`, `TESTS`, `INSTRUCTIONS`, and `LOOKUP_TABLES`.                             |
| 12.16 | -        | ATTACHMENTS and DESTINATIONS parsed but not documented                                                                               | Addressed                 | `ABL_SPEC.md` now documents top-level `ATTACHMENTS` syntax and adds a first-class `DESTINATIONS` section with fields and SSRF-sensitive validation notes.                               |
| 12.17 | -        | DO syntax not documented in abl spec                                                                                                 | Addressed                 | `ABL_SPEC.md` now documents `DO:` as the canonical ordered action-list form for action handlers and canonical digression handlers.                                                      |
| 12.18 | -        | ON_RETURN: action legal values (resume_intent, continue) not documented                                                              | Addressed                 | `ABL_SPEC.md` now lists `ON_RETURN.action` legal values `continue` and `resume_intent` with intended semantics.                                                                         |
| 12.19 | -        | ON_RETURN action/map nesting shown incorrectly in spec                                                                               | Addressed                 | Current `ABL_SPEC.md` examples place `action`/`handler` parallel to `map`, not nested under `map`.                                                                                      |
| 12.20 | -        | Spec should note COMPLETE: WHEN dangerous for Flow agents                                                                            | Addressed                 | `ABL_SPEC.md` now advises FLOW authors to prefer step-local `COMPLETE_WHEN` and terminal `THEN: COMPLETE` over top-level `COMPLETE: WHEN`.                                              |
| 12.21 | -        | GATHER+ON_INPUT interaction order not documented                                                                                     | Addressed                 | `ABL_SPEC.md` now states that FLOW `GATHER` runs before `ON_INPUT` and recommends splitting steps when gather and branch ordering matters.                                              |
| 12.22 | -        | Flow step execution order not documented                                                                                             | Addressed                 | `ABL_SPEC.md` now includes a flow step execution-order table covering RESPOND, GATHER, CALL, CHECK, ON_RESULT, ON_SUCCESS/ON_FAIL, ON_INPUT, and THEN.                                  |
| 12.23 | -        | Advanced GATHER properties undocumented                                                                                              | Addressed                 | `ABL_SPEC.md` now includes an Advanced GATHER Properties table covering semantics, range/list/preferences, activation, dependencies, prompt mode, validation process, and retry prompt. |
| 12.24 | -        | ON_INPUT conditions and intent keywords table should be in spec                                                                      | Addressed                 | `ABL_SPEC.md` now includes ON_INPUT condition pattern examples and a built-in intent keyword table.                                                                                     |
| 12.25 | -        | MEMORY section needs clearer intro on session vs user scoping                                                                        | Addressed                 | `ABL_SPEC.md` now introduces session, user, project, and execution-tree memory scopes directly in the MEMORY section.                                                                   |
| 12.26 | -        | user_id not documented as system variable                                                                                            | Addressed                 | `ABL_SPEC.md` now marks `user_id`, `project_id`, and `tenant_id` as reserved system identifiers.                                                                                        |
| 12.27 | -        | MEMORY forms (READS/WRITES, ACCESS: readwrite, INITIAL, RESET) not fully documented                                                  | Addressed                 | `ABL_SPEC.md` now documents compatibility `READS`/`WRITES`, `ACCESS: readwrite`, and session `INITIAL`/`RESET` hints.                                                                   |
| 12.28 | -        | Consider allowing CLEAR inside ON_FAIL in global CONSTRAINTS                                                                         | Addressed by design close | `ABL_SPEC.md` now explicitly states that `CLEAR` is not a structured global-constraint `ON_FAIL` directive and recommends routing to a flow step for explicit clearing.                 |
| 12.29 | -        | Allow THEN: COMPLETE in global digressions                                                                                           | Addressed                 | `ABL_SPEC.md` now states that `THEN: COMPLETE` is legal in global and step-level digressions when the flow should terminate.                                                            |
| 12.30 | -        | Avoid top-level constraints on GATHER variables — prefer field-level VALIDATION                                                      | Addressed                 | `ABL_SPEC.md` now recommends field-level validation for intrinsic gathered-value checks and reserves top-level constraints for cross-field/tool/handoff business rules.                 |
| 12.31 | -        | Spec claims 35 built-in functions but lists 36                                                                                       | Addressed                 | `ABL_SPEC.md` no longer hardcodes the built-in function count; it points to the canonical table below instead.                                                                          |
| 12.32 | -        | Runtime overview documentation needed for ABL spec readers                                                                           | Addressed                 | ABL_SPEC now includes execution pipeline, AgentIR compilation/runtime wiring, and multi-agent orchestration sections that orient readers before the detailed examples.                  |
| 13.1  | -        | Supervisor passes session.customer_id in handoffs but it's never gathered                                                            | Addressed                 | BankNexus now derives `customer_id` from authenticated context on `ON_START` and passes `customer_id`/workflow memory grants instead of relying on an ungathered `session.customer_id`. |
| 13.2  | -        | Supervisor missing AGENTS list                                                                                                       | Addressed                 | `examples/banknexus/agents/BankNexus_Supervisor.agent.abl` now declares `AGENTS` entries for balance, transfer, and history specialists.                                                |
| 13.3  | -        | Handoff to "Live_Human_agent" should use ESCALATE                                                                                    | Addressed                 | BankNexus README and supervisor/specialist agents now use `ESCALATE` for human assistance; no `Live_Human_agent` handoff remains in the example.                                        |
| 13.4  | -        | persistent: declarations with no REMEMBER are meaningless                                                                            | Addressed                 | BankNexus persistent workflow paths are now read via `memory_grants` and written through `workflow.*` `SET` statements, so the declarations are no longer inert placeholders.           |
| 13.5  | -        | Transfer agent missing tool to get accounts list                                                                                     | Addressed                 | `Fund_Transfer` now declares `get_accounts` and begins with `fetch_source_accounts`/`choose_source_account` before collecting recipient details.                                        |
| 13.6  | -        | PII not masked in review_transfer — recipient_name exposed                                                                           | Addressed                 | `review_transfer` masks source and recipient account numbers and no longer renders `recipient_name` in the customer-facing transfer summary.                                            |
| 13.7  | -        | transfer_failed step ignores user request to speak to agent                                                                          | Addressed                 | `transfer_failed` now gathers `retry_choice` and escalates when the input contains `agent`, `human`, or `support`.                                                                      |
| 13.8  | -        | ESCALATE context_for_human references values never set                                                                               | Partially addressed       | Most BankNexus escalation context values are now initialized or runtime-managed, but `routing_history` is still declared and passed by the supervisor without a verified writer.        |

## Apr 19-May 21 feedback

| ID   | Severity | Item                                                                                                                                                                                         | Current status            | Recovery note                                                                                                                                                                                                                                         |
| ---- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1  | CRITICAL | nodesToDocuments silently discards canvas edits on export                                                                                                                                    | Open / verified gap       | `packages/editor/src/utils/conversion.ts` still rebuilds export from frozen `data.document` values and does not reconstruct step/routing/edge edits from canvas node state.                                                                           |
| 2.1  | HIGH     | Unscoped Redis callback key uses deterministic ID                                                                                                                                            | Addressed                 | `WorkflowCallbackHandler` persists callbacks under `buildRedisKey(tenantId, projectId, executionId)` and uses Redis `SET ... EX ... NX` for duplicate protection.                                                                                     |
| 3.1  | CRITICAL | HANDOFF CONTEXT.pass silently loses GATHER type annotations                                                                                                                                  | Addressed                 | Handoff pass fields now resolve to structured `ResolvedPassField` entries with `type`/`description`, covered by `pass-field-resolution.test.ts`.                                                                                                      |
| 3.2  | HIGH     | DELEGATE has no compile-time type-safety at all                                                                                                                                              | Partially addressed       | Delegate target and return-field validation plus CEL-in-input warnings now exist, but no verified child-input type/field contract validation was found.                                                                                               |
| 3.3  | MEDIUM   | HANDOFF/DELEGATE interface inconsistency                                                                                                                                                     | Addressed                 | Parser support and regression tests now cover unified `TO:`, `PASS:`, and `SUMMARY:` syntax across HANDOFF and DELEGATE.                                                                                                                              |
| 4.1  | HIGH     | MEMORY vs GATHER type conflict silently ignored                                                                                                                                              | Open / verified gap       | Existing validation covers gather/NLU type mismatches and missing producers, but no focused MEMORY-vs-GATHER same-name type conflict diagnostic was found.                                                                                            |
| 4.2  | HIGH     | ENTITIES section not documented in ABL_SPEC                                                                                                                                                  | Addressed                 | `ABL_SPEC.md` now includes a first-class `ENTITIES` section with syntax, field table, and gather reference rules.                                                                                                                                     |
| 4.3  | HIGH     | Handoff conditions fail on null vs false comparisons                                                                                                                                         | Open / verified gap       | Bounded compiler/runtime searches found `COALESCE` support and examples, but no automatic normalization for handoff boolean conditions that compare null vs false.                                                                                    |
| 5.1  | HIGH     | user.requests_human referenced in spec/examples but never set by runtime                                                                                                                     | Open / verified gap       | Current references are in docs/tests; no runtime assignment path for `user.requests_human` was found.                                                                                                                                                 |
| 5.2  | HIGH     | System variable naming inconsistency                                                                                                                                                         | Partially addressed       | `ABL_SPEC.md` now distinguishes canonical snake_case runtime variables from host-supplied dotted `user.*` metadata, but existing examples still mix both styles.                                                                                      |
| 5.3  | HIGH     | System variables not documented in ABL_SPEC                                                                                                                                                  | Addressed                 | `ABL_SPEC.md` now aligns the system-variable table with the canonical runtime registry, including `input`, `last_input`, `user_id`, `project_id`, `tenant_id`, `abl`, and `result`.                                                                   |
| 6.1  | HIGH     | PII protection agent uses manual inadequate patterns and inverts truth test                                                                                                                  | Addressed                 | `examples/guardrails/agents/pii_protection.agent.abl` now uses `abl.contains_pii(...)`, and ABL_SPEC documents guardrail `check` expressions as violation predicates.                                                                                 |
| 7.1  | MEDIUM   | Constraints doc should explain WHEN constraints are checked and variable clearing                                                                                                            | Partially addressed       | ABL_SPEC now states constraints are flattened and evaluated every turn, with `BEFORE` checkpoints; constraint-specific variable-clearing guidance remains incomplete.                                                                                 |
| 8.1  | MEDIUM   | No per_activation reset for memory variables                                                                                                                                                 | Open / verified gap       | No `per_activation` memory reset contract or runtime implementation was found in the bounded pass.                                                                                                                                                    |
| 9.1  | -        | Handoff ON_FAILURE should be renamed ON_FAIL for consistency                                                                                                                                 | Addressed by design close | Current contract keeps handoff `ON_FAILURE`; docs/tests define phase-aware semantics instead of renaming to `ON_FAIL`.                                                                                                                                |
| 9.2  | -        | HANDOFF type adjustment to string is irrelevant (recanted)                                                                                                                                   | No action item            | Source marks this as recanted; no remediation required.                                                                                                                                                                                               |
| 9.3  | -        | LLM for gather validation — batch multiple validations per call?                                                                                                                             | Open / verified gap       | Gather docs/tests expose LLM extraction/validation concepts, but no verified runtime batching path for multiple LLM gather validations was found.                                                                                                     |
| 10.1 | EX       | Uses old-style RETURN instead of EXPECT_RETURN                                                                                                                                               | Addressed                 | Old-style `RETURN` is still accepted; `EXPECT_RETURN` is preferred in docs/examples.                                                                                                                                                                  |
| 10.2 | EX       | Supervisor handoff to Device_support doesn't pass serial_number                                                                                                                              | Partially addressed       | `examples/apple-care/agents/apple_care_supervisor.agent.abl` now passes `serial_number` to `Device_Support`, but the docs-internal by-industry snippet still omits it.                                                                                |
| 10.3 | EX       | Escalate test on undefined variable                                                                                                                                                          | Partially addressed       | The source Apple-care supervisor now escalates on declared `data_loss_risk`; the docs-internal by-industry snippet still references undefined `device_issue_involves_data_loss`.                                                                      |
| 10.4 | EX       | IMPLIES constraint tests condition that can never be true at that point                                                                                                                      | Open / verified gap       | `examples/apple-care/agents/account_support.agent.abl` still checks `action == "disable" IMPLIES two_factor_risk_acknowledged == true` without verified producers for those fields.                                                                   |
| 10.5 | EX       | ON_INPUT contains-based routing is silly when THEN would suffice                                                                                                                             | Open / verified gap       | Apple-care repair flow examples still use keyword-heavy `ON_INPUT` branches for simple routing choices where direct transitions/options would be clearer.                                                                                             |
| 10.6 | EX       | "Genius bar" referenced — Apple-specific term shouldn't be in generic example                                                                                                                | Addressed                 | No current `Genius bar`/`genius` reference was found outside the Bruce feedback source itself.                                                                                                                                                        |
| 11.1 | EX       | Additional issues on top of prior BankNexus feedback (see Mar 22-Apr 18 doc)                                                                                                                 | Partially addressed       | The Mar 22-Apr 18 BankNexus items were re-audited in this report; most are addressed, with `routing_history` escalation context still partial.                                                                                                        |
| 12.1 | EX       | SENSITIVE_DISPLAY parsed but runtime doesn't use it                                                                                                                                          | Partially addressed       | `SENSITIVE_DISPLAY`/`PII_TYPE` support exists for gather/display paths; full lower-level masking unification remains partial.                                                                                                                         |
| 12.2 | EX       | ENTITIES section missing from ABL_SPEC entirely                                                                                                                                              | Addressed                 | Same as 4.2: `ABL_SPEC.md` now has a canonical `ENTITIES` section with parser/compiler-backed syntax and rules.                                                                                                                                       |
| 13.1 | EX       | Supervisor has no AGENTS declaration                                                                                                                                                         | Addressed                 | `examples/env-demo/agents/EnvDemo_Supervisor.agent.abl` now declares `AGENTS: stock_lookup: Stock_Lookup`.                                                                                                                                            |
| 13.2 | EX       | Handoff passes undefined variable `query` — dead code                                                                                                                                        | Addressed                 | Env-demo supervisor now passes only `session_context` to `Stock_Lookup`; no `query` pass-through remains in the example.                                                                                                                              |
| 13.3 | EX       | System ignores undefined variable in CONTEXT.pass (no error)                                                                                                                                 | Partially addressed       | Compiler field-reference validation now treats handoff `context.pass` as a consumer and emits `MISSING_VARIABLE_PRODUCER_WARNING`, but it is still warning-level rather than error.                                                                   |
| 14.1 | EX       | COMPLETE condition can never match — crawl_batch.status and all_browser_pages_visited both unreachable at runtime                                                                            | Addressed                 | `web_crawler.agent.abl` now completes on `last_crawl_batch_result`, `last_crawl_batch_respond_result`, or `last_get_crawl_status_result` fields rather than the unreachable legacy names.                                                             |
| 15.1 | EX       | "This example is going nowhere" — various undefined variables: profile_id, customer_info, dispute_context, conversation_history, escalation_reason, escalation_required, contact_to_merchant | Partially addressed       | DisputeTransaction now initializes `profile_id` and removes `customer_info`/`dispute_context`/`conversation_history`, but `escalation_required`/`dispute_id` still lack verified producers and `customerId`/`customer_id` usage remains inconsistent. |
| 16.1 | EX       | No agent is a supervisor — Welcome_Agent should be                                                                                                                                           | Addressed                 | The AI4H payer-provider example now has `SUPERVISOR: Healthcare_Supervisor` as the routing entrypoint.                                                                                                                                                |
| 16.2 | EX       | EXECUTION max_flow_iterations: 200 — default is 100, why doubled with no comment?                                                                                                            | Addressed                 | Current AI4H payer-provider agents use normal `max_iterations` values; no `max_flow_iterations: 200` remains in the example.                                                                                                                          |
| 17.1 | EX       | Supervisor passes undefined variables: customer_id, order_id, item_id, cart_contents, manager_approved                                                                                       | Open / verified gap       | `retail_supervisor.agent.abl` now initializes `customer_id`, but still passes `order_id`, `item_id`, and `cart_contents` without verified producers; `sales_agent.agent.abl` also gates on `manager_approved` without a producer.                     |
| 17.2 | EX       | user.wants_human doesn't exist — handoff always fails                                                                                                                                        | Partially addressed       | Current retail source uses `user.wants_human_agent`, but internal example docs still include stale `user.wants_human` handoff conditions.                                                                                                             |
| 18.1 | EX       | Legacy ON_START in Recall should use current recall event syntax                                                                                                                             | Addressed                 | Current examples and `ABL_SPEC.md` use canonical recall events such as `ON: session:start`; bounded search found no legacy `ON_SESSION_START` recall example.                                                                                         |
| 18.2 | EX       | abl_spec should document recall event ON/ACTION/PATHS meanings                                                                                                                               | Addressed                 | `ABL_SPEC.md` now documents RECALL event patterns plus the meanings of `ON`, `ACTION`, `PATHS`, `DOMAIN`, and `INSTRUCTION`.                                                                                                                          |
| 18.3 | EX       | network_triage_agent: alarm_id assumed passed by supervisor but never set                                                                                                                    | Addressed                 | `noc_supervisor.agent.abl` now declares `alarm_id` as a gathered session field and passes it to `Network_Triage`, whose own `GATHER alarm_id` is required.                                                                                            |
| 18.4 | EX       | Step calls multiple CALLs sequentially — not documented whether that's legal                                                                                                                 | Addressed                 | Current telco agents use one `CALL` per flow step with explicit `THEN` sequencing; `network_triage.agent.abl` splits alarm details, inventory, affected services, and acknowledgement into separate steps.                                            |
| 18.5 | MEDIUM   | Undefined variable handling — system silently passes null to tools without error                                                                                                             | Partially addressed       | Field-reference validation now emits `MISSING_VARIABLE_PRODUCER_WARNING` for unknown tool-input and expression variables, but the diagnostic is still warning-level and runtime remains permissive.                                                   |
| 19.1 | EX       | Handoff conditions fail on null vs false for user.is_validated                                                                                                                               | Addressed                 | `examples/saludsa/agents/supervisor.agent.abl` now routes validation with `user.is_validated != true`, so null/undefined users still enter the validation handoff.                                                                                    |
| 19.2 | EX       | Whatsapp_User_Check routing uses undefined state                                                                                                                                             | Partially addressed       | `Whatsapp_User_Check` now declares and sets local routing state (`phone_verified`, `phone_not_linked`, `identity_verified`), but the broader supervisor contract still does not clearly reconcile those child outcomes into `user.is_validated`.      |
| 20.1 | DOC      | Various issues in handbook HTML documentation (forwarded from Harsha)                                                                                                                        | Open / verified gap       | Static handbook/anatomy HTML files still exist under `apps/studio/public/agent-anatomy/`; no recovered issue breakdown or closure trail was found for Harsha's forwarded handbook concerns.                                                           |

## Highest-Signal Remaining Work

1. Close or deliberately re-park the confirmed open runtime/compiler items: DSL `LOG`, missing `context_for_human` warnings, `retry_step` backtrack limits, backtrack reset, guardrail `modified` boolean/trace, NLU substring fallback, configurable default language, and streaming full-buffer guardrail evaluation.
2. Close the remaining Mar 20-21 implementation items: SmartAssist retry off-by-one, deflect-to-chat XO branch alignment, generic CSAT return-flow verification, Arch true streaming, and Arch conversation-key/projectId persistence.
3. Close the verified example/docs drift that remains after the audit: retail pass-through state, DisputeTransaction variable/tool-contract drift, Apple-care/docs-internal stale snippets, Saludsa validation-state reconciliation, and handbook/anatomy HTML follow-up.
