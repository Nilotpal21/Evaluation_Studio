# LLD Log — Cross-Provider Quorum & Planning Convergence

**Feature**: cross-provider-quorum-convergence
**Ticket**: ABLP-406
**Artifact**: `docs/plans/2026-04-19-cross-provider-quorum-convergence-impl-plan.md`

---

## 2026-04-19 — Phase 1: Prerequisites

Read in fresh Phase 1 pass (all required artifacts present, none missing):

- `docs/features/sub-features/cross-provider-quorum-convergence.md` (STATUS PLANNED, 16 FRs, 23 test scenarios in §17, 3-phase delivery plan in §13).
- `docs/specs/cross-provider-quorum-convergence.hld.md` (STATUS DRAFT, commit `9b9768964`, 3 audit rounds passed, all 12 concerns addressed with Option C chosen).
- `docs/testing/sub-features/cross-provider-quorum-convergence.md` (STATUS PLANNED, 8 E2E + 11 integration + 10 unit + 3 security + 3 perf).
- `packages/helix/HELIX.md` (vision doc, §Future Work #1 names the `openai-api` gap).
- `packages/helix/CLAUDE.md` (operational rules, change-checklist tests list).
- `packages/helix/agents.md` (learning journal including the 2026-04-19 HLD entries: SpecialStageExecutor signature uniformity, holistic-audit static-const dispatch-time override, addJournalEntry public vs appendToJournalFile private, HLD line-drift tolerance).

Source code anchors re-verified during Phase 1 (line numbers stable vs. HLD Round 3 verification):

- `packages/helix/src/types.ts:269-273` — `ModelEngine` union declares `'openai-api'` (executor missing).
- `packages/helix/src/types.ts:281-298` — `ExecutorEfficiencyBudget.disableToolUse` at line 290 already exists.
- `packages/helix/src/types.ts:1210-1233` — `HelixConfig` interface; `sessionDir` at line 1216 (public string field).
- `packages/helix/src/models/model-router.ts:48-67` — executor map hardcoded in constructor (registers `claude-code` + `codex-cli`).
- `packages/helix/src/models/model-router.ts:202-227` — `executeSpec` returns `ExecutorResult` with `error` field for unavailable engine (does NOT throw).
- `packages/helix/src/models/model-router.ts:245-248` — `registerExecutor(executor: ModelExecutor)` public method (post-construction registration path).
- `packages/helix/src/oracles/oracle-constellation.ts:355-412` — four `defaultOracles` definitions; Architecture is the second entry at 370-383 with `engine: 'claude-code', model: 'opus', maxBudgetUsd: 10`.
- `packages/helix/src/pipeline/pipeline-engine.ts:1800-1868` — `executeStage` dispatch tower (user-checkpoint, bootstrap, implementation slices, oracle-analysis, manifest-compilation, bulk-review, doc-sync, concerns-audit, deterministic-replay, substages, main loop). No `plan-generation` case today.
- `packages/helix/src/pipeline/pipeline-engine.ts:511-515` — `handleBlockingStageResult` call site (FR-8 abort path).
- `packages/helix/src/pipeline/special-stage-executor.ts:42-52` — `SpecialStageExecutorDeps` struct + `SpecialStageExecutor` constructor with single `deps` param.
- `packages/helix/src/pipeline/special-stage-executor.ts:113-166` — `executeVerificationBootstrap` canonical pattern: `(session, stage, startTime, stageDeadlineAt?)` signature, `getRemainingTimeoutMs` guard, `failStageDueToTimeout` on expiry, `this.deps.sessionManager.persist`, `this.deps.journal`, `makeResult` at return.
- `packages/helix/src/pipeline/templates/holistic-audit.ts:19,123` — `PLAN_GENERATION_TIMEOUT_MS` module const at line 19; template exported as top-level `const` at line 123.
- `packages/helix/src/session/session-manager.ts:96` — `persist(session)` public.
- `packages/helix/src/session/session-manager.ts:206` — `addJournalEntry(…)` public.
- `packages/helix/src/session/session-manager.ts:310-312` — `sessionPath` is PRIVATE; consumers must use `path.join(config.sessionDir, session.id)` for ad-hoc artifact writes.

No prerequisite blockers. Proceed to Phase 2.

---

## 2026-04-19 — Phase 2: Clarifying Questions (product-oracle)

Spawned **product-oracle** with full question set (17 questions, grouped). Oracle read feature spec, HLD, test spec, and source code under `packages/helix/`. Result: **11 ANSWERED, 3 DECIDED, 3 INFERRED, 0 AMBIGUOUS**.

### Implementation Strategy (questions 1-5)

| #   | Question                                                                      | Classification | Answer / Decision                                                                                                                                                                                                                                                                            |
| --- | ----------------------------------------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Preferred order — executor first, oracle swap, dueling planners, or parallel? | ANSWERED       | Executor → router registration → oracle swap → dueling flow. HLD §3.2 already serializes it; same ordering as feature spec §13 Phase 1 / Phase 2.                                                                                                                                            |
| 2   | Existing patterns to follow?                                                  | ANSWERED       | `ClaudeSdkExecutor` is the canonical `ModelExecutor` pattern — dynamic import per-call inside `execute()` at claude-sdk-executor.ts:124, no caching. `CodexCliExecutor` is the DI-via-spawner pattern (informs test doubles). `executeVerificationBootstrap` is the canonical special-stage. |
| 3   | Feature flag for phased rollout?                                              | ANSWERED       | No flag service — HELIX has none. Two independent `HelixConfig` booleans plus env-var overrides. Default false for both. Documented in HLD §4 row #10 and feature spec §11.                                                                                                                  |
| 4   | Phase 1 scope limit vs. later phases?                                         | DECIDED        | Oracle classifies as DECIDED because feature spec §13 already scopes 3 phases and HLD preserves that split. Phase 1 = executor + oracle swap + cost. Phase 2 = dueling planners. Phase 3 = docs/rollout. No intra-phase re-slicing proposed.                                                 |
| 5   | Hard deadlines driving phasing?                                               | ANSWERED       | None. HELIX is an internal developer tool; rollout is opt-in via flags. No PRD deadline.                                                                                                                                                                                                     |

### Technical Details (questions 6-10)

| #   | Question                                   | Classification | Answer / Decision                                                                                                                                                                                                                                                                                                                |
| --- | ------------------------------------------ | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 6   | Exact files for modification vs. creation? | ANSWERED       | Feature spec §10 enumerates: 3 NEW (`openai-api-executor.ts`, `engine/execute-dueling-plan-generation.ts`, `engine/dueling-plan-synthesis-prompt.ts`) + 8 UPDATE (types, router, oracle-constellation, special-stage-executor, pipeline-engine, runtime-config + cli, holistic-audit, stage-output-schema).                      |
| 7   | Test-first or test-after?                  | DECIDED        | Test-alongside per-commit. Each commit lands code + tests for its slice. Feature spec §17 + test spec §10 have Phase 1 subset (UT-1..UT-10, INT-1, INT-2) as the pre-BETA witness.                                                                                                                                               |
| 8   | Type/interface changes?                    | ANSWERED       | (a) `ModelEngine` union is already correct — no change. (b) `HelixConfig` gains 3 optional fields. (c) `Session` gains 2 optional fields. (d) `PlanArtifact` is a NEW interface. (e) `StageOutputSchemaId` union gains `'plan-c-with-divergence'`. (f) New named error classes (`OpenAiApiError`, `StructuredOutputParseError`). |
| 9   | Database migration strategy?               | ANSWERED       | N/A — HELIX has no database. JSON round-trip preserves absent fields (verified in HLD §5.5). Sessions written before this feature forwards-compatibly initialize with `?? {}` at read sites.                                                                                                                                     |
| 10  | Performance-sensitive paths?               | ANSWERED       | Two: (a) streaming event mapper (UT-2, PERF-2 — must not buffer full stream); (b) parallel fan-out (PERF-1 — must be genuinely concurrent, not accidentally sequential). Both are pure functions or `Promise.allSettled` — testable as such.                                                                                     |

### Risk & Dependencies (questions 11-17)

| #   | Question                                                                      | Classification | Answer / Decision                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ----------------------------------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 11  | Other ongoing changes on target files?                                        | INFERRED       | `git log --oneline --since "30 days" packages/helix/` shows ~30 recent commits, mostly ABLP-406 refactor (holistic-audit decomposition, pipeline-engine orchestrator extraction). All additive. Branch-safety assessment: LOW risk. No concurrent feature touching these files.                                                                                                                                                         |
| 12  | Biggest implementation risk?                                                  | ANSWERED       | **Codex synthesis fidelity**. If Codex with `disableToolUse: true` fails to emit both Plan C + divergence-notes in the expected schema, INT-9 (Plan Quality gate flow-through) fails. Mitigation: schema parser rejection triggers existing plan-review retry loop (FR-14). GAP-007 tracks first-run validation.                                                                                                                        |
| 13  | Team dependencies (review/approval)?                                          | ANSWERED       | HELIX maintainers only. No cross-team review. `packages/helix/CLAUDE.md` change-checklist dictates test-file updates.                                                                                                                                                                                                                                                                                                                   |
| 14  | Monitoring/alerting prerequisites?                                            | ANSWERED       | None beyond what HLD §4 concern #8 specifies (progress events, session JSON, artifact files, journal line, MCP `get_session` surface). No dashboard, no alerting. GAP-006 tracks.                                                                                                                                                                                                                                                       |
| 15  | Definition of done for the whole feature?                                     | DECIDED        | **BETA promotion** = all 23 test spec scenarios pass (§17) + Phase 3 doc-sync done. **ALPHA promotion** = UT-1..UT-10 + INT-1 + INT-2 + at least 3 of E2E-1..E2E-8 pass. Matches feature spec §17 "ALPHA→BETA threshold when scenarios 1-12 and at least 3 of 13-18 pass."                                                                                                                                                              |
| 16  | `sessionManager.sessionPath` helper exposed?                                  | ANSWERED       | No — `sessionPath` is PRIVATE (session-manager.ts:310). Consumers compute `path.join(config.sessionDir, session.id)` directly. `config.sessionDir` is a public `string` field on `HelixConfig` (types.ts:1216). Used already across several call sites in `packages/helix/src/pipeline/` for ad-hoc artifact writes.                                                                                                                    |
| 17  | Cost accumulator placement — post-hook on `ModelRouter` or call-site wrapper? | DECIDED        | **Call-site wrapper**. `ModelRouter.execute` has NO session reference — it operates on `ModelAssignment`, not `Session`. Adding a session reference would require touching every `modelRouter.execute()` call site anyway. Cleaner as a pure function `accumulateProviderCost(session, result)` called at each `modelRouter.execute(...)` return site in `pipeline-engine.ts` and `special-stage-executor.ts`. UT-8 locks the contract. |

### DECIDED-for-LLD corollaries (derived from oracle answers)

- **D-1 (multi-commit-in-one-PR)**: Phase 1 split into 3 commits (types + config + executor + router; oracle-swap + cost accumulator; tests). Phase 2 split into 3 commits (types + schema + synthesis-prompt; dueling executor + pipeline dispatch + timeout override; integration tests + E2E). Phase 3 = single docs commit. Each commit ≤ 40 files and ≤ 3 packages per CLAUDE.md commit-scope guard — HELIX is one package.
- **D-2 (cost accumulator as pure function)**: `accumulateProviderCost(session, executorResult): void` is exported from a new `packages/helix/src/pipeline/cost-accumulator.ts` module. All call sites invoke it immediately after the `modelRouter.execute()` Promise resolves. No new `await` added — accumulator is synchronous pure function.
- **D-3 (feature-spec errata fixed in LLD Phase 3, not deferred)**: Feature spec line 278 references the wrong signature (`executeDuelingPlanGeneration(workDir, session, stage, deps)`) and line 282 references a non-existent `planStageTimeoutMs(config)` helper. Neither shape is used in the HLD or LLD. Phase 3 doc-sync task updates feature-spec §10 file map and §13 Phase 2 task 2.6 to match LLD reality. Not deferred to separate `/post-impl-sync`.

### Open items flagged for LLD Phase 3 generation

- None. All 17 questions resolved from the artifacts and source code.

Proceed to Phase 3.

---

## 2026-04-19 — Phase 4b: Audit Round 1 (lld-reviewer, architecture compliance)

**Verdict**: NEEDS_REVISION
**Focus**: Stateless/distributed state, traceability, failure modes, error handling, config surface, secrets, no scope creep.

### Findings resolved in this round

| Sev      | Finding                                                                                                                                                                                                 | Resolution                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CRITICAL | Promise.allSettled classification breaks because `ModelRouter.execute()` returns `ExecutorResult` with `error` field — never throws. Both-fail scenario would fall through to Codex synthesis silently. | Phase 2 Step 3 rewritten: each `.then()` branch inspects `result.error`; if non-empty, throws a typed wrapper (`ClaudeSdkError` / `OpenAiApiError`) so `Promise.allSettled` classifies as `rejected`. D-9 rewritten to scope error classes to the orchestrator wrapper layer only — executors continue error-as-data contract. Task 1.A.5 marked removed-with-rationale (no retrofit to executor throw sites).          |
| HIGH     | `accumulateProviderCost` call-site count misstated (4-6) — actual 9 sites at lines 1982, 2935, 4035, 4197, 4251, 4705, 5185, 5328, 5384.                                                                | Task 1.B.3 updated with exact 9 line numbers. New coverage guard UT-8b added (Task 1.C.5) to assert every call-site path increments `costByProvider`. Site at line 5384 requires tail-return conversion (`const result = await ...; accumulate; return result;`).                                                                                                                                                       |
| HIGH     | Error class retrofit changed existing executor error contract without updating 9+ callers that branch on `result.error` (ModelRouter fallback line 109, pipeline-engine 3 sites, etc.).                 | D-9 rewritten: typed classes are instantiated only inside `execute-dueling-plan-generation.ts`. Task 1.A.5 converted to a "do-not-modify" task with explicit rationale. `BudgetExceededError` is the only in-place raise and is caught within the executor before crossing the ModelRouter boundary.                                                                                                                    |
| HIGH     | `abortSignal` passed as 7th argument to `modelRouter.execute()` does not exist in the signature (6 parameters at `model-router.ts:84-91`).                                                              | Phase 2 Step 3(a) and Step 6 updated: six-parameter signature (`prompt, assignment, tools, onStream, outputSchema, timeoutMs`). Pipeline abort documented as `deps.modelRouter.abortActiveExecutions()` at line 175.                                                                                                                                                                                                    |
| MEDIUM   | Partial resume gap — no branch handled `planA && !planB` (mid-fanout interrupt) despite E2E-8 asserting only Planner B is re-invoked.                                                                   | Step 2 gained two new branches: `planA && !planB` → launch only B (via `{ launchA: false, launchB: true }` descriptor), and symmetric `planB && !planA` → launch only A. Step 3 wraps the single-planner case in `Promise.allSettled([singleCall])` for uniform classification. Step 4 treats `0 rejected + 1 fulfilled` as Solo-pass when descriptor indicates single-launch. Test matrix gained two new resume cells. |
| MEDIUM   | Concurrent persist guarantee explanation was incorrect (`.then()` awaits don't serialize siblings).                                                                                                     | Step 3 rewritten: guarantee now reads "both branches mutate the same in-memory session object; SessionManager persist queue serializes disk writes". INT-10 assertion clarified to reload from disk and check both artifacts present.                                                                                                                                                                                   |
| MEDIUM   | `outputSchema` Codex invocation used bare literal `'plan-c-with-divergence'` — actual `StageOutputSchemaConfig` is `{ id, strict? }`.                                                                   | Step 6 now uses `{ id: 'plan-c-with-divergence' }`.                                                                                                                                                                                                                                                                                                                                                                     |
| LOW      | Test DI override mechanism not specified alongside the production executor registration.                                                                                                                | Task 1.A.6 gained an explicit note: tests call `modelRouter.registerExecutor(new OpenAiApiExecutor(workDir, fakeFactory))` (public `registerExecutor` at `model-router.ts:245-248` overwrites the constructor-registered instance).                                                                                                                                                                                     |
| LOW      | HELIX logging convention (`process.stderr.write`) not stated for new modules.                                                                                                                           | Task 1.A.7 added: all new modules write diagnostic output only via `process.stderr.write` (no `console.log`, no `createLogger`). Documented in `packages/helix/agents.md` 2026-04-03 entry.                                                                                                                                                                                                                             |
| LOW      | `helix doctor` message may confuse operators re: pre-existing Codex dependency on OPENAI_API_KEY.                                                                                                       | Not in scope for this feature — pre-existing behavior acknowledged in finding.                                                                                                                                                                                                                                                                                                                                          |

### Verified-clean items (round 1)

- Scoping (tenant/project/user N/A for HELIX CLI, documented in HLD §4 #1 and feature spec §12).
- `sessionDir` path computation matches existing pattern at `pipeline-engine.ts:424-425` and `cli.ts:521`.
- Secrets handling: `OPENAI_API_KEY` read from env only; SEC-1/2/3 cover non-persistence and non-logging.
- No console.log / custom auth / sync I/O.
- Scope matches HLD/feature spec — no new capabilities.
- All 12 referenced source files exist with line numbers +/-5.
- `ModelExecutor` interface, `SpecialStageExecutorDeps`, and `ModelRouter.execute` signatures match source.
- Failure-mode traceability: FR-7, FR-8, FR-9 all have explicit `StageResult` mappings and advisory entries.
- Idempotency on resume: duelingPlanState checkpoint reuse covers all 4 branches (full, mid-fanout×2, synthesis, complete) post-round-1.

Round 1 complete. 1 CRITICAL + 3 HIGH + 3 MEDIUM + 2 LOW all resolved (LOW #2 acknowledged as out-of-scope). Proceed to Round 2 (pattern consistency).

## 2026-04-19 — Phase 4b: Audit Round 2 (lld-reviewer, pattern consistency)

**Verdict**: NEEDS_REVISION
**Focus**: Pattern consistency against HELIX house style (executor shape, special-stage executor, dispatch tower, oracle constellation, cost accumulator, schema registration, config/CLI, tests, journal/progress events, stderr convention, module DAG, reinvention check, round-1 regression).

### Round-1 fixes verified (auditor confirmed all 10 in place)

1. D-9 rewritten with error-as-data preservation
2. Task 1.A.5 marked removed-with-rationale
3. Task 1.B.3 lists 9 call sites by line number
4. Phase 2 Step 3 wraps `result.error` into typed class before `Promise.allSettled`
5. Phase 2 Step 6 removes `abortSignal` and uses `{ id: 'plan-c-with-divergence' }`
6. New resume branches `planA && !planB` and `planB && !planA`
7. Concurrent-persist explanation corrected
8. Task 1.A.6 mentions test DI override via `registerExecutor`
9. Task 1.A.7 documents stderr convention
10. Resume-after-Planner-A-only and Resume-after-Planner-B-only test cases added

### New findings resolved in this round

| Sev    | Finding                                                                                                                                                                                | Resolution                                                                                                                                                                                                                                                                                                                                                                                            |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HIGH   | Round-1 residual: Modified Files table and Commit 1.A Files Touched list still referenced executor throw-site updates that contradict D-9/Task 1.A.5.                                  | §2.2 rows for `claude-sdk-executor.ts` and `codex-cli-executor.ts` rewritten to "NOT MODIFIED per D-9"; Commit 1.A Files Touched list edited to remove both files, with an explicit "(Intentionally NOT touched ...)" note.                                                                                                                                                                           |
| HIGH   | Task 2.A.1/2.A.2 used Zod `.extend()` / `.safeParse()` syntax. HELIX schemas are raw `JsonSchemaDocument` objects validated via AJV 8 (see `stage-output-schema.ts:1-22,111-147`).     | Task 2.A.1 rewritten with a full `planCWithDivergenceSchema: JsonSchemaDocument` skeleton cloning `slicePlanSchema`'s top-level shape and adding `divergenceNotes: { type: 'string' }`. Task 2.A.2 rewritten to use `validateStageOutputData({ id: 'plan-c-with-divergence', strict })` per the AJV pattern. §2.2 entry for `stage-output-schema.ts` rewritten to "raw `JsonSchemaDocument`, no Zod". |
| HIGH   | `buildOracleSynthesisModelSpec` (`oracle-constellation.ts:777-800`) hardcodes `model: 'claude-sonnet-4-6'` — crashes when Architecture oracle is swapped to `openai-api`.              | Task 1.B.2 extended with an explicit fix: engine-aware fallback. New D-15 added: `'claude-sonnet-4-6'` for claude-code, `'gpt-4o-mini'` for openai-api. Config threaded through `OracleConstellation` instance to enable future `openaiSynthesisRetryModel` escape hatch.                                                                                                                             |
| HIGH   | `inferOracleConfidence` (`oracle-constellation.ts:1220-1229`) only recognizes `'opus'` and `'sonnet'`. GPT-5 would default to 0.68 (lowest tier), silently degrading quorum weighting. | Task 1.B.2 extended to add GPT-5 (0.82, parity with opus) and GPT-4o (0.75, above default) tiers. New D-16 documents the full confidence table and rationale.                                                                                                                                                                                                                                         |
| MEDIUM | `src/__tests__/test-helpers/` directory does not exist today.                                                                                                                          | Task 1.C.1 gained a note: "first entry under `src/__tests__/test-helpers/`; `mkdir -p` required; shared-fixture pattern endorsed by agents.md 2026-04-19 for 3+ consumer test files".                                                                                                                                                                                                                 |
| MEDIUM | `OracleConstellation` constructor doesn't accept `HelixConfig` — Task 1.B.2 deferred the threading decision.                                                                           | Task 1.B.2 committed to: add `config?: HelixConfig` as the 5th constructor parameter; update line 66 to `this.oracles = customOracles ?? buildDefaultOracles(config)`; caller at `special-stage-executor.ts:196-201` passes `this.deps.config`.                                                                                                                                                       |
| MEDIUM | `defaultOracles` is module-private; Task 1.B.2 presented two alternatives instead of picking one.                                                                                      | Task 1.B.2 committed: convert `const defaultOracles` to `function buildDefaultOracles(config?: HelixConfig)`. Keep module-private. `resolveArchitectureOracle(config)` is the only new export.                                                                                                                                                                                                        |
| LOW    | `MODEL_PRICING_USD` data structure not specified (could be `Map`, blocked by unbounded-collections hook).                                                                              | Task 1.A.4 explicitly states: plain `Record<string, { inputUsdPer1M, outputUsdPer1M, reasoningUsdPer1M? }>`. No `Map`.                                                                                                                                                                                                                                                                                |
| LOW    | `DuelingPlanGenerationDeps` vs `StageTimeoutSideEffects` structural compatibility not called out.                                                                                      | Phase 2 Step 1 of Task 2.B.1 extended with a note: TS structural typing allows `deps` to be passed directly to `failStageDueToTimeout(stage, startTime, deps)`.                                                                                                                                                                                                                                       |

Round 2 complete. 4 HIGH + 3 MEDIUM + 2 LOW all resolved. Two new decisions added (D-15, D-16). Proceed to Round 3 (completeness: every FR covered, file paths verified, signatures checked).

## Phase 4b: Audit Round 3 (lld-reviewer, completeness)

- Agent: lld-reviewer
- Round: 3 of 5
- Focus: every FR covered, file paths verified, signatures checked
- Verdict: NEEDS_REVISION → resolved in-round

### Findings resolved in this round

| Sev    | Finding                                                                                                                                                                                                                       | Resolution                                                                                                                                                                                                                                                                                                                                                                |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HIGH   | Task 2.C.4 still referenced "Zod schema" and `.safeParse()` assertions, contradicting D-8 and Task 2.A.1's AJV-based schema registration.                                                                                     | Task 2.C.4 rewritten to assert `schemaById['plan-c-with-divergence']` is a plain `JsonSchemaDocument`, `$schema`/`$id`/`type`/`properties`/`required` populated, `validateStageOutputData({ id, strict: true })` surfaces AJV errors for malformed input. Explicit "No Zod anywhere." marker added.                                                                       |
| HIGH   | Task 1.B.4 claimed env-var parsing lives in `runtime-config.ts`, but grep confirms zero env reads there. Real pattern lives in `workspace-context.ts:9-55` (`env.HELIX_*`).                                                   | Task 1.B.4 split: `runtime-config.ts` gets defaults only; `workspace-context.ts` gets the 3 new `HELIX_*` env reads following its existing pattern. Alternative path (cli.ts `buildHelixConfig`) documented. §2.2 Modified Files table updated. Commit 1.B Files Touched extended to include `workspace-context.ts`. Wiring-checklist item 12 corrected.                  |
| MEDIUM | Task 1.B.5 put the new readiness check directly in `cli.ts`; actual entry is `runHelixDoctor` in `packages/helix/src/readiness/doctor.ts` (cli.ts only dispatches at lines 58, 164, 400, 1218).                               | Task 1.B.5 rewritten: `cli.ts` keeps the 3 new flags and threads them into `runHelixDoctor` options; readiness check added to `readiness/doctor.ts` alongside the existing check set. `formatReadinessSummary` renders it; `cli.ts` exit-code behavior unchanged. Commit 1.B Files Touched extended to include `readiness/doctor.ts`. Wiring-checklist item 13 corrected. |
| MEDIUM | Oracle cost-accumulator insertion site misnamed `runOracleDefinition` with no scope for `session`; real site is `executeOracleReview` (method line 262, `modelRouter.execute` line 282), and `session` is not in scope there. | Task 1.B.6 rewritten with two concrete threading options: (a) thread `session` as an extra arg to `executeOracleReview`; (b) return raw `ExecutorResult` and accumulate one level up in `analyzeFindings` (line 71). Implementer picks — document in commit message.                                                                                                      |
| MEDIUM | HLD §4 Security Surface (line 360) says "Zod-based `stage-output-parsers.ts`" and HLD §7 Audit Logging (line 524) says "Zod schemas in `stage-output-schema.ts`" — both false post-D-8.                                       | Task 3.5a added: during Phase 3 doc-sync, rewrite HLD line 360 to reference AJV 8 + JSON Schema draft 2020-12, and rewrite line 524 to reference `JsonSchemaDocument` + `validateStageOutputData`. Commit 3 Files Touched extended to include the HLD.                                                                                                                    |
| LOW    | `COST_ATTRIBUTION_FIXTURE` consumers not enumerated — fixture author wouldn't know the consumer contract.                                                                                                                     | Task 1.C.1 annotated: `pipeline-engine.test.ts` (INT-5 + UT-8b) and `oracle-constellation.test.ts` (INT-3).                                                                                                                                                                                                                                                               |
| LOW    | §10 References section referenced `runtime-config.ts:108` env-driven pattern that does not exist (line 108 is unrelated code).                                                                                                | §10 References edited: `runtime-config.ts` described as "defaults only"; env-driven overrides credited to `workspace-context.ts:9-55`.                                                                                                                                                                                                                                    |

Round 3 complete. 2 HIGH + 3 MEDIUM + 2 LOW all resolved. No new decisions added. All file paths in the LLD now verified against current source. Proceed to Round 4 (phase-auditor, cross-phase consistency — LLD implements HLD, covers every test-spec scenario, matches feature-spec delivery plan).

## Phase 4b: Audit Round 4 (phase-auditor, cross-phase consistency)

- Agent: phase-auditor
- Round: 4 of 5
- Focus: LLD implements HLD 12 concerns, covers test-spec scenarios, matches feature-spec delivery plan
- Verdict: APPROVED with 2 HIGH + 2 MEDIUM + 1 LOW (no CRITICAL) — all resolved in-round

### Verified clean

- All 12 HLD architectural concerns addressed (11 implemented, 1 N/A with justification matching HLD).
- HLD Option C (dueling planners + architecture oracle swap) faithfully implemented.
- All 16 feature-spec FRs mapped to LLD tasks.
- 3-phase structure aligns with feature-spec §13 delivery plan.
- D-3 errata scheduled for Phase 3 Task 3.1 correction.
- Round-3 resolutions (Zod→AJV, workspace-context.ts, readiness/doctor.ts) internally consistent.
- D-9 error-as-data contract preserved — executors NOT modified.
- 33 of 35 test-spec scenarios explicitly mapped (remaining 2 = UT-8, INT-6; fixed below).
- Scope lock: PASS. No scope creep.
- Terminology consistency: PASS across all 4 artifacts.
- packages/helix/agents.md learnings reflected in LLD.

### Findings resolved in this round

| Sev    | Finding                                                                                                                                                                                                                                                         | Resolution                                                                                                                                                                                                                                                                                                                                                    |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HIGH   | UT-8 (4 pure-function sub-scenarios on `accumulateProviderCost`) not explicitly named in any Phase 1 task or test-strategy summary.                                                                                                                             | Task 1.C.5 extended: new bullet enumerates the 4 UT-8 sub-scenarios as pure-function assertions against `pipeline/cost-accumulator.ts`, co-located in `pipeline-engine.test.ts` unless size forces a dedicated `accumulate-provider-cost.test.ts`. Phase 1 Test Strategy Unit list edited to include `UT-8`.                                                  |
| HIGH   | INT-6 (plan-stage timeout dispatch integration test) not mapped to any Phase 2 task or test file, although wiring-checklist already references UT-6 / INT-6.                                                                                                    | Task 2.C.5 extended: new bullet maps INT-6 to `pipeline-engine.test.ts` covering (a) timeout read at stage-entry, (b) `{ status: 'timeout' }` return via `handleStageTimeout`, (c) non-boolean `enableDuelingPlanners` falls back to 8-min default with one `process.stderr.write` warning. Phase 2 Test Strategy Integration list edited to include `INT-6`. |
| MEDIUM | Task 2.B.3 did not warn implementers that feature-spec §13 Phase 2 task 2.6 is errata — an implementer consulting the feature-spec delivery plan could mistakenly edit `holistic-audit.ts`.                                                                     | Task 2.B.3 now opens with an IMPLEMENTER NOTE flagging the feature-spec errata (references D-3 / D-6), explicitly stating `holistic-audit.ts` is NOT to be modified, and noting the errata correction ships in Phase 3 Task 3.1.                                                                                                                              |
| MEDIUM | Two references to "E2E scenario 23 from feature spec §17" conflated the feature-spec §17 numeric row matrix (rows 1-23) with the test-spec's `E2E-N` namespace (which has no `E2E-23`).                                                                         | Wiring-checklist item 18 (line 717) and final acceptance-criteria bullet (line 788) rewritten: both now read "feature-spec §17 row 23" and name the covering tests (UT-9 + INT-8) instead of a non-existent `E2E-23`.                                                                                                                                         |
| LOW    | D-10 Rationale column still said "`runtime-config.ts:108` already threads env-driven config overrides" despite the round-3 correction establishing that `runtime-config.ts` has zero env reads and the real pattern lives in `workspace-context.ts` lines 9-55. | D-10 Rationale edited: now reads "`workspace-context.ts` (lines 9-55) already threads env-driven `HELIX_*` config overrides … (Corrected round-4 — round-3 established that `runtime-config.ts` contains no env reads.)"                                                                                                                                      |

Round 4 complete. 2 HIGH + 2 MEDIUM + 1 LOW all resolved. No scope changes, no new decisions. Cross-phase traceability now complete: every FR, HLD concern, and test-spec scenario mapped into the LLD. Proceed to Round 5 (lld-reviewer final sweep — task independence, wiring checklist, domain rules).

## Phase 4b: Audit Round 5 (lld-reviewer, final sweep)

- Agent: lld-reviewer
- Round: 5 of 5
- Focus: task independence, wiring checklist, domain rules, round 1-4 resolutions stuck, commit discipline, exit criteria measurable, rollback plans real
- Verdict: APPROVED with 1 HIGH + 2 MEDIUM (no CRITICAL) — all resolved in-round

### Verified clean

- Architecture compliance: HELIX CLI (N/A tenant/project/user), no console.log, no custom auth, no unbounded Map (MODEL_PRICING_USD is a `Record`), stateless JSON.
- Pattern consistency: `OpenAiApiExecutor` matches `ClaudeSdkExecutor` dynamic-import-per-call; `executeDuelingPlanGeneration` matches `executeVerificationBootstrap` signature; AJV schemas (no Zod); cost accumulator is pure function.
- Error-as-data contract (D-9): preserved; executors NOT modified; typed error classes scoped to orchestrator wrapper.
- Completeness: all 16 FRs, all 35 test scenarios, all file paths verified.
- Domain rules: HELIX logging stderr-only, no unbounded Map/Set, single package, no userId in cache keys.
- Task independence: Phase 1/2 commits are strictly serial by dependency; no file overlap between independent commits.
- Commit discipline: 7 commits, single package, largest commit touches 8 files, all within hook caps, all feat() commits additive (zero deletions).
- Wiring checklist: all 21 items specific with file:line references and named tests.
- Exit criteria: all measurable (specific vitest commands, tsc checks, CLI invocations with expected outputs, specific test scenario IDs).
- Rollback plans: real — exact commits named, residual state described (JSON fields harmless, config defaults preserve behavior).
- Round 1-4 resolutions: all 30 prior findings stuck in current document.

### Findings resolved in this round

| Sev    | Finding                                                                                                                                                                                                                                                                                                                                                                   | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HIGH   | Task 1.A.2 added `'plan-c-with-divergence'` to the `StageOutputSchemaId` union in Phase 1, but the matching `schemaById` entry lands in Task 2.A.1 (Phase 2). `schemaById: Record<StageOutputSchemaId, JsonSchemaDocument>` requires every union member as a key, so shipping the union literal in Phase 1 breaks `tsc --noEmit` and violates the Phase 1 exit criterion. | Task 1.A.2 rewritten: literal removed from Phase 1 scope, with explicit rationale ("`Record<K,V>` requires every union member as key"). Task 2.A.1 now opens "Update `types.ts` + `pipeline/stage-output-schema.ts` **atomically in this commit**" and lists the union extension as its first bullet. §2.2 Modified Files row for `types.ts` split into Phase 1 / Phase 2 responsibilities. §1.2 types sample block annotated. Commit 2.A Files Touched list extended to include `types.ts`. |
| MEDIUM | Task 2.B.1 Step 1 shorthand `return failStageDueToTimeout(stage, startTime, deps)` reads as a 3-param call, but the real signature (`fail-stage-due-to-timeout.ts:40-54`) takes 9 params. An implementer following the shorthand literally gets a TS compile error.                                                                                                       | Step 1 now spells out the full 9-arg call: `failStageDueToTimeout(session, stage, '', [], [], startTime, 1, {}, deps)` with an inline key per argument. The structural-typing justification for `deps` as arg 9 is preserved.                                                                                                                                                                                                                                                                |
| MEDIUM | Task 1.B.4 described extending "the existing workspace-context record"; the module actually exports standalone per-group resolver functions (`resolveCliWorkspaceContext`, `resolveInitialLiveContext`, `resolveReplayContext`), not a single record.                                                                                                                     | Task 1.B.4 rewritten: new sibling function `resolveHelixFeatureFlags(env = process.env): { useOpenAiArchitectureOracle?, enableDuelingPlanners?, openaiModel? }` added to `workspace-context.ts` following the existing per-group pattern; `cli.ts` `buildHelixConfig()` invokes it and merges with CLI-flag > env > default precedence. Rejected alternative (inline env reads in `buildHelixConfig` without the helper) documented.                                                        |

Round 5 complete. 1 HIGH + 2 MEDIUM all resolved. No CRITICAL findings. Rounds 1-4 resolutions remain stuck. The LLD is implementer-ready. Total findings across 5 rounds: 1 CRITICAL, 10 HIGH, 9 MEDIUM, 5 LOW — all resolved in-round with 3 new decisions added (D-15, D-16, plus strengthened D-9). Proceed to commit and chain into `/implement`.
