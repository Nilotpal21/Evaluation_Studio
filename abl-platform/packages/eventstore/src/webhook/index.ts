/**
 * Event webhook forwarder - forwards events to tenant subscriptions.
 */

export {
  EventWebhookForwarder,
  type WebhookSubscription,
  type WebhookForwarderConfig,
} from './event-webhook-forwarder.js';
