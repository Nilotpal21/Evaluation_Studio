/**
 * MCP Server Authentication Types
 *
 * Re-exports from @agent-platform/shared-kernel for backwards compatibility.
 */
export type { McpAuthConfig, McpAuthType } from '@agent-platform/shared-kernel';
export { MCP_AUTH_TYPES } from '@agent-platform/shared-kernel';
