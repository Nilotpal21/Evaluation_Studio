/**
 * Repository Types
 *
 * Re-exports from @agent-platform/shared-kernel for backwards compatibility.
 */
export type { PaginatedResponse, ErrorResult, Result } from '@agent-platform/shared-kernel';
