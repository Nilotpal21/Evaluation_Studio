# Arch AI as Observer

## TODO

- Auto-watch sessions and runtime logs of actual agent executions
- Consume analytics insights from real production/session data
- Generate draft observations on agent behavior patterns, failures, and inefficiencies
- Produce proposals for agent improvements (prompt tuning, tool usage, flow optimization)
- Produce proposals for project-level improvements (configuration, model selection, knowledge gaps)
