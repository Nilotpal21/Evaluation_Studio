# CLI Driver — Parked Work (ABLP-162)

> Phase 1 of `kore-platform-cli arch *` shipped. Implementation: `packages/kore-platform-cli/src/commands/arch.ts`. Docs: `packages/kore-platform-cli/README.md`. Architecture rationale: `packages/arch-ai/agents.md` § "2026-05-07 — Headless CLI driver".

The following items were deliberately deferred. Pick up only when the listed trigger condition fires — premature work has no value here.

## Phase 2 — Extract shared HTTP client into `packages/agent-platform-client/`

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **What**             | Move `lib/api-client.ts`, `lib/credentials.ts`, `lib/config.ts` patterns from `packages/kore-platform-cli/` into a new shared package. Both `kore-platform-cli` and `mcp-debug` consume it.                                                                                                                                                                                                                      |
| **Trigger**          | When `mcp-debug`'s auth client diverges from `kore-platform-cli`'s, OR a third consumer needs the same plumbing, OR auth flow changes need to be applied in both places.                                                                                                                                                                                                                                         |
| **Estimated effort** | 0.5 day. Pure refactor — no behavior change.                                                                                                                                                                                                                                                                                                                                                                     |
| **Risk**             | `mcp-debug` is a published npm package (`@koredotcom/agents-mcp-tools`). Refactor must keep its public exports stable. Bump minor version.                                                                                                                                                                                                                                                                       |
| **Pickup checklist** | (1) Identify the exact files to move (`client/`, `utils/`, `store/` from mcp-debug + `lib/api-client.ts`, `lib/credentials.ts`, `lib/config.ts` from kore-platform-cli — careful, these are NOT identical today, they're parallel implementations). (2) Choose the canonical implementation per file. (3) Add subpath exports if needed. (4) Migrate consumers. (5) Verify both binaries still pass smoke tests. |

## Phase 3 — Headless server (`packages/arch-ai-server/`)

|                      |                                                                                                                                                                                                                                                                                                                                              |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **What**             | Extract `apps/studio/src/lib/arch-ai/processors/process-message.ts` + `process-in-project.ts` + tool builders (~15,800 LOC) into a non-Next.js Express server. CLI takes `--server-url localhost:7777` to point at it instead of Studio.                                                                                                     |
| **Trigger**          | When Studio HMR proves too slow for CLI iteration in real use. Today: not an issue (Studio HMR is sub-second for route handlers).                                                                                                                                                                                                            |
| **Estimated effort** | 3–5 days. Heavy refactor — touches Studio orchestration, model resolution, file storage, file upload, project access guards.                                                                                                                                                                                                                 |
| **Risk**             | High — replicates a load-bearing surface. Must maintain parity with Studio's auth + tenant isolation + project access. Worth doing only with strangler-pattern migration (run both in parallel, diff outputs, cut over when green).                                                                                                          |
| **Pickup checklist** | (1) Confirm Studio HMR really is the bottleneck — measure. (2) Decide whether to extract orchestration or run the same Studio code under Express. (3) Plan auth — reuse `requireTenantAuth` shape or rebuild from `@agent-platform/shared`? (4) Plan file storage — GridFS or filesystem? (5) Strangler-mode parity testing before cut over. |

## Bonus — SSE response headers on Studio

|                      |                                                                                                                                                                                                                                 |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **What**             | Add `X-Accel-Buffering: no` and `Connection: keep-alive` to the SSE response in `apps/studio/src/app/api/arch-ai/message/route.ts:325`. Currently only `Content-Type: text/event-stream` and `Cache-Control: no-cache` are set. |
| **Trigger**          | When deploying Studio behind nginx or a reverse proxy that buffers responses by default. CLI ↔ direct localhost is unaffected.                                                                                                  |
| **Estimated effort** | One line of code. Separate ticket, separate commit — orthogonal to the CLI work.                                                                                                                                                |
| **Risk**             | None — purely additive headers. No existing client should regress.                                                                                                                                                              |
