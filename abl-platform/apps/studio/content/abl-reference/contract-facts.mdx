---
title: 'ABL Contract Facts'
section: 'abl-reference'
order: 95
description: 'Generated factual reference for built-in functions, lifecycle events, coordination actions, and system variables.'
---

> This page is generated by `pnpm abl:docs:generate`.
> Do not edit it directly; update the contract registry instead.

## Overview

This generated reference captures code-backed ABL facts from the compiler layer. The current foundation slice focuses on registry-backed public surfaces, built-in functions, lifecycle events, coordination semantics, and system variable behavior.

## Stability Tiers

| Tier           | Summary                                                           | Guidance                                                                                                      |
| -------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `core`         | Public contract intended for production use.                      | Core constructs must stay aligned across parser, compiler, runtime, docs, and examples.                       |
| `beta`         | Public but still evolving; consumers should expect guided change. | Beta constructs should be documented and validated, but may require migration notes or compatibility lanes.   |
| `experimental` | Not a stable public contract yet.                                 | Experimental constructs should warn clearly in tooling and must not be treated as guaranteed portable syntax. |

## Public Surfaces

| Surface                            | Scope   | Stability      | Support   | Summary                                                                                                                                                                                                                 |
| ---------------------------------- | ------- | -------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `builtins.resolve_value`           | runtime | `core`         | `wired`   | Pure and impure helper functions available to value resolution and interpolation.                                                                                                                                       |
| `handoff.context.history`          | handoff | `core`         | `wired`   | Controls how much parent conversation history is forwarded during handoff; the platform default is auto, which prefers summary_only when a real handoff summary exists and otherwise falls back to bounded raw history. |
| `handoff.on_return`                | handoff | `beta`         | `partial` | Configures how a parent resumes after a child handoff through a structured action/handler object with optional field mapping.                                                                                           |
| `handoff.return_handlers`          | handoff | `beta`         | `wired`   | Named parent-resume handlers let RETURN:true handoffs clear state and optionally emit a follow-up response without inventing pseudo-agent targets.                                                                      |
| `memory.recall.events`             | memory  | `beta`         | `wired`   | Canonical lifecycle event names used by recall hooks.                                                                                                                                                                   |
| `handoff.context.memory_grants`    | handoff | `beta`         | `wired`   | Explicit cross-agent grants expose only the named memory paths to the receiving agent with declared access semantics.                                                                                                   |
| `memory.persistent.execution_tree` | memory  | `beta`         | `wired`   | Durable workflow-scoped memory shared across one execution or handoff tree, isolated from broader user/project persistence.                                                                                             |
| `lookup_tables.agent_local`        | runtime | `experimental` | `wired`   | Agent-local lookup tables remain experimental; project runtime config lookup_tables are the canonical shared source for lookup-backed gathers.                                                                          |
| `tooling.generated.doc.mirrors`    | tooling | `core`         | `wired`   | Canonical app doc mirrors are generated from shared source documents and contract facts.                                                                                                                                |

## Built-in Functions (36)

| Category      | Count | Functions                                                                                           |
| ------------- | ----- | --------------------------------------------------------------------------------------------------- |
| math          | 8     | `ABS`, `ADD`, `DIV`, `MAX`, `MIN`, `MUL`, `ROUND`, `SUB`                                            |
| string        | 10    | `JOIN`, `LOWER`, `PAD_END`, `PAD_START`, `REPEAT`, `REPLACE`, `SPLIT`, `SUBSTRING`, `TRIM`, `UPPER` |
| formatting    | 4     | `FORMAT_CURRENCY`, `FORMAT_DATE`, `MASK`, `ORDINAL`                                                 |
| type_coercion | 5     | `IS_ARRAY`, `IS_NUMBER`, `IS_STRING`, `TO_NUMBER`, `TO_STRING`                                      |
| array         | 3     | `ARRAY_FIND`, `ARRAY_FIND_INDEX`, `LENGTH`                                                          |
| object        | 3     | `OBJECT_KEYS`, `OBJECT_MERGE`, `OBJECT_VALUES`                                                      |
| utility       | 3     | `COALESCE`, `NOW`, `UNIQUE_ID`                                                                      |

### Function Purity

| Name               | Category      | Pure? |
| ------------------ | ------------- | ----- |
| `ABS`              | math          | yes   |
| `ADD`              | math          | yes   |
| `DIV`              | math          | yes   |
| `MAX`              | math          | yes   |
| `MIN`              | math          | yes   |
| `MUL`              | math          | yes   |
| `ROUND`            | math          | yes   |
| `SUB`              | math          | yes   |
| `JOIN`             | string        | yes   |
| `LOWER`            | string        | yes   |
| `PAD_END`          | string        | yes   |
| `PAD_START`        | string        | yes   |
| `REPEAT`           | string        | yes   |
| `REPLACE`          | string        | yes   |
| `SPLIT`            | string        | yes   |
| `SUBSTRING`        | string        | yes   |
| `TRIM`             | string        | yes   |
| `UPPER`            | string        | yes   |
| `FORMAT_CURRENCY`  | formatting    | yes   |
| `FORMAT_DATE`      | formatting    | yes   |
| `MASK`             | formatting    | yes   |
| `ORDINAL`          | formatting    | yes   |
| `IS_ARRAY`         | type_coercion | yes   |
| `IS_NUMBER`        | type_coercion | yes   |
| `IS_STRING`        | type_coercion | yes   |
| `TO_NUMBER`        | type_coercion | yes   |
| `TO_STRING`        | type_coercion | yes   |
| `ARRAY_FIND`       | array         | yes   |
| `ARRAY_FIND_INDEX` | array         | yes   |
| `LENGTH`           | array         | yes   |
| `OBJECT_KEYS`      | object        | yes   |
| `OBJECT_MERGE`     | object        | yes   |
| `OBJECT_VALUES`    | object        | yes   |
| `COALESCE`         | utility       | yes   |
| `NOW`              | utility       | no    |
| `UNIQUE_ID`        | utility       | no    |

## Lifecycle / Recall Events

| Canonical Event            | Legacy Aliases | Stability | Description                                  |
| -------------------------- | -------------- | --------- | -------------------------------------------- |
| `session:start`            | -              | `core`    | New session begins.                          |
| `session:end`              | -              | `core`    | Session ends.                                |
| `agent:<name>:before`      | -              | `beta`    | Before a named agent executes.               |
| `agent:<name>:after`       | -              | `beta`    | After a named agent completes.               |
| `agent:*:before`           | -              | `beta`    | Before any agent executes.                   |
| `agent:*:after`            | -              | `beta`    | After any agent completes.                   |
| `tool:<name>:after`        | -              | `beta`    | After a named tool executes.                 |
| `tool:*:after`             | -              | `beta`    | After any tool executes.                     |
| `entity:<field>:extracted` | -              | `beta`    | After an entity or field value is extracted. |
| `step:enter:<name>`        | -              | `beta`    | When a flow step is entered.                 |
| `step:exit:<name>`         | -              | `beta`    | When a flow step is exited.                  |

## Handoff History Strategies

| Strategy       | Syntax                              | Stability | Support | Description                                                                                                                                                                 |
| -------------- | ----------------------------------- | --------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auto`         | `auto`                              | `core`    | `wired` | Prefer the authored handoff summary when it is present and safe to use; otherwise fall back to the last 5 messages (or the project override). This is the platform default. |
| `none`         | `none`                              | `core`    | `wired` | Do not pass summary or raw message history. Use this only for an intentionally fresh child context.                                                                         |
| `summary_only` | `summary_only`                      | `core`    | `wired` | Pass only the summary field and no raw message history.                                                                                                                     |
| `full`         | `full`                              | `beta`    | `wired` | Pass summary plus the full available message history.                                                                                                                       |
| `last_n`       | `{ mode: last_n, count: <number> }` | `beta`    | `wired` | Pass summary plus only the last N messages. The legacy shorthand `last_<n>` remains accepted during the compatibility window.                                               |

## Coordination Actions

| Surface              | Syntax              | Stability | Support | Semantics                                                     |
| -------------------- | ------------------- | --------- | ------- | ------------------------------------------------------------- |
| `handoff.on_timeout` | `continue`          | `core`    | `wired` | Continue in the parent after timeout without escalating.      |
| `handoff.on_timeout` | `escalate`          | `core`    | `wired` | Escalate to the configured human/system target after timeout. |
| `handoff.on_timeout` | `respond:<message>` | `beta`    | `wired` | Return a custom response message when the handoff times out.  |
| `handoff.on_return`  | `continue`          | `beta`    | `wired` | Continue the parent agent after the child returns.            |
| `handoff.on_return`  | `resume_intent`     | `beta`    | `wired` | Resume the parent intent path after the child returns.        |

## System Variables

| Name                                | Surfaces                                  | Stability      | Support | Description                                                                                                                             |
| ----------------------------------- | ----------------------------------------- | -------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `_abl_constraint_checkpoint_kind`   | `field_reference`                         | `experimental` | `wired` | Internal structural checkpoint kind for constraint evaluation.                                                                          |
| `_abl_constraint_checkpoint_target` | `field_reference`                         | `experimental` | `wired` | Internal structural checkpoint target for constraint evaluation.                                                                        |
| `abl`                               | `field_reference`                         | `core`         | `wired` | ABL helper-function namespace available in expression and guardrail conditions.                                                         |
| `always`                            | `field_reference`                         | `core`         | `wired` | Built-in truthy sentinel for unconditional expressions.                                                                                 |
| `channel`                           | `field_reference`                         | `core`         | `wired` | Current channel identifier available to expression validation/runtime context.                                                          |
| `customer_id`                       | `field_reference`                         | `beta`         | `wired` | Current customer/contact identifier when the runtime has resolved one.                                                                  |
| `input`                             | `field_reference`                         | `core`         | `wired` | Current user input value.                                                                                                               |
| `intent`                            | `field_reference`                         | `core`         | `wired` | Current detected intent value when available.                                                                                           |
| `language`                          | `field_reference`                         | `core`         | `wired` | Current language code.                                                                                                                  |
| `last_<tool_name>_result`           | `session_value_pattern`                   | `core`         | `wired` | Raw tool result stored in session values when tool execution leaves result storage enabled.                                             |
| `last_input`                        | `field_reference`                         | `core`         | `wired` | Previous user input value when available.                                                                                               |
| `locale`                            | `field_reference`                         | `core`         | `wired` | Current locale value.                                                                                                                   |
| `previous_system_message_was_offer` | `field_reference`                         | `experimental` | `wired` | Internal offer-tracking flag exposed to expression validation.                                                                          |
| `project_id`                        | `field_reference`                         | `beta`         | `wired` | Current project identifier when the runtime has resolved scoped project context.                                                        |
| `result`                            | `field_reference`                         | `beta`         | `wired` | Current result variable reference when populated by execution flow.                                                                     |
| `session_id`                        | `field_reference`, `tool_param_injection` | `core`         | `wired` | Current session identifier; also auto-injected into tool params when declared in the tool schema.                                       |
| `tenant_id`                         | `field_reference`, `tool_param_injection` | `beta`         | `wired` | Current tenant identifier; also auto-injected into tool params when declared in the tool schema.                                        |
| `turn_count`                        | `field_reference`                         | `core`         | `wired` | Current conversation turn count.                                                                                                        |
| `user_id`                           | `field_reference`, `tool_param_injection` | `beta`         | `wired` | Current authenticated user identifier. This is system-owned session context and should be treated as immutable in public ABL authoring. |

## Compatibility Notes

| ID                                 | Category  | Summary                                                                                                                     | Guidance                                                                                                                                                                                             |
| ---------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `handoff.history-default`          | `runtime` | When no explicit history strategy is declared, handoffs default to `auto`.                                                  | Use `summary_only` only when the handoff provides a real summary and the child should not receive raw history. `auto` keeps that behavior when safe and otherwise falls back to bounded raw history. |
| `handoff.machine-targets`          | `runtime` | HANDOFF targets machine agents only: a compiled local agent or a declared remote agent endpoint.                            | Use `ESCALATE` for human/system resolution. If you need a machine orchestration step before human transfer, hand off to a real agent such as `Live_Agent_Transfer`, then let that agent escalate.    |
| `handoff.history-last-n-shorthand` | `syntax`  | Legacy DSL `history: last_<n>` remains accepted and lowers into the canonical structured form `{ mode: last_n, count: n }`. | Prefer the typed authoring form `history: { mode: last_n, count: n }` in new docs and generated YAML. Keep `last_<n>` only as a compatibility input.                                                 |
| `docs.generated-mirrors`           | `docs`    | App-facing full-specification pages are generated from canonical docs sources.                                              | Edit `docs/reference/ABL_SPEC.md` and rerun `pnpm abl:docs:generate` instead of editing app mirror files directly.                                                                                   |

## Source Files

- `packages/compiler/src/platform/contracts/abl-contract-registry.ts`
- `packages/compiler/src/platform/constructs/executors/tool-binding-executor.ts`
- `packages/compiler/src/platform/constructs/evaluator.ts`
- `packages/compiler/src/platform/constants.ts`
- `packages/compiler/src/platform/ir/validate-coordination-config.ts`
- `packages/compiler/src/platform/ir/validate-field-refs.ts`
