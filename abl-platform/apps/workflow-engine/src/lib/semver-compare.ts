/**
 * Semver descending comparator — re-exported from the canonical shared-kernel
 * implementation so workflow-engine and runtime share a single source of truth.
 */

export { compareSemverDesc } from '@agent-platform/shared-kernel';
