/**
 * Runtime executes the shared MCP registry contract suite via import so this
 * package cannot drift from packages/shared on future behavior changes.
 */

import '../../../../packages/shared/src/__tests__/mcp-server-registry.test.ts';
