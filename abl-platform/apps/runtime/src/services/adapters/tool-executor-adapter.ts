/**
 * Tool Executor Adapter (DEPRECATED)
 *
 * MockToolExecutor and MOCK_TOOL_RESPONSES have been removed from production code.
 *
 * For production no-op fallback, use NoOpToolExecutor from:
 *   ../execution/noop-tool-executor.ts
 *
 * For debug/test mock injection (decorator pattern), use MockToolExecutor from:
 *   ../execution/mock-tool-executor.ts
 */
