/**
 * PromptTemplateLoader — Re-exports from @agent-platform/shared/prompts.
 *
 * The canonical implementation lives in the shared package.
 * This file exists for backward compatibility with existing runtime imports.
 */
export { PromptTemplateLoader, promptTemplateLoader } from '@agent-platform/shared/prompts';
