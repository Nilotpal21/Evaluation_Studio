/**
 * OpenAI S2S Provider Stub
 *
 * This is a minimal stub kept for backwards compatibility.
 * Actual S2S integration is done via Jambonz llm verb, not through this provider.
 */

import type { IS2SProvider } from '../types.js';

export class OpenAIS2SProvider implements IS2SProvider {
  // Stub - not used with Jambonz llm verb approach
  constructor() {
    // No-op
  }
}
