# Google OAuth (shared with Studio - same credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# JWT secret for docs sessions (independent from Studio)
# Generate with: openssl rand -base64 64
DOCS_JWT_SECRET=docs-dev-secret-change-in-production

# App URL (for OAuth redirect URI)
DOCS_APP_URL=http://localhost:3007
