Find existing libraries, integrations, and internal packages in the ABL platform that solve the developer's need. Use the `platform-toolkit` skill.

Check before adding any new npm dependency. If an existing library or internal abstraction covers the need, recommend it with the specific import path and usage example.

Categories: HTTP, databases, queues, LLM/AI, document processing, auth, cloud/storage, communication, validation, observability, frontend UI, ABL internals, testing.

$ARGUMENTS
